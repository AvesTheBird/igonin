{"ast":null,"code":"/*\nCopyright 2021-present The maxGraph project Contributors\nCopyright (c) 2006-2019, JGraph Ltd\nCopyright (c) 2006-2017, draw.io AG\n\nLicensed under the Apache License, Version 2.0 (the \"License\");\nyou may not use this file except in compliance with the License.\nYou may obtain a copy of the License at\n\n    http://www.apache.org/licenses/LICENSE-2.0\n\nUnless required by applicable law or agreed to in writing, software\ndistributed under the License is distributed on an \"AS IS\" BASIS,\nWITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.\nSee the License for the specific language governing permissions and\nlimitations under the License.\n*/\nimport Point from '../geometry/Point';\nimport Rectangle from '../geometry/Rectangle';\nimport TemporaryCellStates from '../cell/TemporaryCellStates';\nimport InternalEvent from '../event/InternalEvent';\nimport Client from '../../Client';\nimport { intersects } from '../../util/mathUtils';\nimport { DIALECT } from '../../util/Constants';\nimport { addLinkToHead, write } from '../../util/domUtils';\n/**\n * @class PrintPreview\n *\n * Implements printing of a diagram across multiple pages. The following opens\n * a print preview for an existing graph:\n *\n * ```javascript\n * var preview = new mxPrintPreview(graph);\n * preview.open();\n * ```\n *\n * Use {@link getScaleForPageCount} as follows in order to print the graph\n * across a given number of pages:\n *\n * ```javascript\n * var pageCount = mxUtils.prompt('Enter page count', '1');\n *\n * if (pageCount != null)\n * {\n *   var scale = mxUtils.getScaleForPageCount(pageCount, graph);\n *   var preview = new mxPrintPreview(graph, scale);\n *   preview.open();\n * }\n * ```\n *\n * ### Additional pages\n *\n * To add additional pages before and after the output, {@link getCoverPages} and\n * {@link getAppendices} can be used, respectively.\n *\n * ```javascript\n * var preview = new mxPrintPreview(graph, 1);\n *\n * preview.getCoverPages(w, h)\n * {\n *   return [this.renderPage(w, h, 0, 0, mxUtils.bind(this, function(div)\n *   {\n *     div.innerHTML = '<div style=\"position:relative;margin:4px;\">Cover Page</p>'\n *   }))];\n * };\n *\n * preview.getAppendices(w, h)\n * {\n *   return [this.renderPage(w, h, 0, 0, mxUtils.bind(this, function(div)\n *   {\n *     div.innerHTML = '<div style=\"position:relative;margin:4px;\">Appendix</p>'\n *   }))];\n * };\n *\n * preview.open();\n * ```\n *\n * ### CSS\n *\n * The CSS from the original page is not carried over to the print preview.\n * To add CSS to the page, use the css argument in the {@link open} function or\n * override {@link writeHead} to add the respective link tags as follows:\n *\n * ```javascript\n * var writeHead = preview.writeHead;\n * preview.writeHead(doc, css)\n * {\n *   writeHead.apply(this, arguments);\n *   doc.writeln('<link rel=\"stylesheet\" type=\"text/css\" href=\"style.css\">');\n * };\n * ```\n *\n * ### Padding\n *\n * To add a padding to the page in the preview (but not the print output), use\n * the following code:\n *\n * ```javascript\n * preview.writeHead(doc)\n * {\n *   writeHead.apply(this, arguments);\n *\n *   doc.writeln('<style type=\"text/css\">');\n *   doc.writeln('@media screen {');\n *   doc.writeln('  body > div { padding-top:30px;padding-left:40px;box-sizing:content-box; }');\n *   doc.writeln('}');\n *   doc.writeln('</style>');\n * };\n * ```\n *\n * ### Headers\n *\n * Apart from setting the title argument in the mxPrintPreview constructor you\n * can override {@link renderPage} as follows to add a header to any page:\n *\n * ```javascript\n * var oldRenderPage = renderPage;\n * renderPage(w, h, x, y, content, pageNumber)\n * {\n *   var div = oldRenderPage.apply(this, arguments);\n *\n *   var header = document.createElement('div');\n *   header.style.position = 'absolute';\n *   header.style.top = '0px';\n *   header.style.width = '100%';\n *   header.style.textAlign = 'right';\n *   mxUtils.write(header, 'Your header here');\n *   div.firstChild.appendChild(header);\n *\n *   return div;\n * };\n * ```\n *\n * The pageNumber argument contains the number of the current page, starting at\n * 1. To display a header on the first page only, check pageNumber and add a\n * vertical offset in the constructor call for the height of the header.\n *\n * ### Page Format\n *\n * For landscape printing, use {@link mxConstants.PAGE_FORMAT_A4_LANDSCAPE} as\n * the pageFormat in {@link getScaleForPageCount} and {@link PrintPreview}.\n * Keep in mind that one can not set the defaults for the print dialog\n * of the operating system from JavaScript so the user must manually choose\n * a page format that matches this setting.\n *\n * You can try passing the following CSS directive to {@link open} to set the\n * page format in the print dialog to landscape. However, this CSS\n * directive seems to be ignored in most major browsers, including IE.\n *\n * ```javascript\n * @page {\n *   size: landscape;\n * }\n * ```\n *\n * Note that the print preview behaves differently in IE when used from the\n * filesystem or via HTTP so printing should always be tested via HTTP.\n *\n * If you are using a DOCTYPE in the source page you can override {@link getDoctype}\n * and provide the same DOCTYPE for the print preview if required. Here is\n * an example for IE8 standards mode.\n *\n * ```javascript\n * var preview = new mxPrintPreview(graph);\n * preview.getDoctype()\n * {\n *   return '<!--[if IE]><meta http-equiv=\"X-UA-Compatible\" content=\"IE=5,IE=8\" ><![endif]-->';\n * };\n * preview.open();\n * ```\n */\nclass PrintPreview {\n  constructor(graph) {\n    let scale = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : null;\n    let pageFormat = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : null;\n    let border = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : null;\n    let x0 = arguments.length > 4 && arguments[4] !== undefined ? arguments[4] : 0;\n    let y0 = arguments.length > 5 && arguments[5] !== undefined ? arguments[5] : 0;\n    let borderColor = arguments.length > 6 && arguments[6] !== undefined ? arguments[6] : null;\n    let title = arguments.length > 7 && arguments[7] !== undefined ? arguments[7] : 'Printer-friendly version';\n    let pageSelector = arguments.length > 8 && arguments[8] !== undefined ? arguments[8] : null;\n    /**\n     * The border inset around each side of every page in the preview. This is set\n     * to 0 if autoOrigin is false.\n     * @default 0\n     */\n    this.border = 0;\n    /**\n     * The margin at the top of the page (number).\n     * @default 0\n     */\n    this.marginTop = 0;\n    /**\n     * The margin at the bottom of the page (number).\n     * @default 0\n     */\n    this.marginBottom = 0;\n    /**\n     * Holds the horizontal offset of the output.\n     */\n    this.x0 = 0;\n    /**\n     * Holds the vertical offset of the output.\n     */\n    this.y0 = 0;\n    /**\n     * Specifies if the origin should be automatically computed based on the top,\n     * left corner of the actual diagram contents. The required offset will be added\n     * to {@link x0} and {@link y0} in {@link open}.\n     * @default true\n     */\n    this.autoOrigin = true;\n    /**\n     * Specifies if overlays should be printed.\n     * @default false\n     */\n    this.printOverlays = false;\n    /**\n     * Specifies if controls (such as folding icons) should be printed. Default is\n     * false.\n     */\n    this.printControls = false;\n    /**\n     * Specifies if the background image should be printed.\n     * @default false\n     */\n    this.printBackgroundImage = false;\n    /**\n     * Holds the color value for the page background color.\n     * @default '#ffffff'\n     */\n    this.backgroundColor = '#ffffff';\n    /**\n     * Reference to the preview window.\n     */\n    this.wnd = null;\n    /**\n     * Assign any window here to redirect the rendering in {@link open}.\n     */\n    this.targetWindow = null;\n    /**\n     * Holds the actual number of pages in the preview.\n     */\n    this.pageCount = 0;\n    /**\n     * Specifies is clipping should be used to avoid creating too many cell states\n     * in large diagrams. The bounding box of the cells in the original diagram is\n     * used if this is enabled.\n     * @default true\n     */\n    this.clipping = true;\n    this.graph = graph;\n    this.scale = scale != null ? scale : 1 / graph.pageScale;\n    this.border = border != null ? border : 0;\n    this.pageFormat = Rectangle.fromRectangle(pageFormat != null ? pageFormat : graph.pageFormat);\n    this.title = title;\n    this.x0 = x0;\n    this.y0 = y0;\n    this.borderColor = borderColor;\n    this.pageSelector = pageSelector != null ? pageSelector : true;\n  }\n  /**\n   * Returns {@link wnd}.\n   */\n  getWindow() {\n    return this.wnd;\n  }\n  /**\n   * Returns the string that should go before the HTML tag in the print preview\n   * page. This implementation returns an X-UA meta tag for IE5 in quirks mode,\n   * IE8 in IE8 standards mode and edge in IE9 standards mode.\n   */\n  getDoctype() {\n    const dt = '';\n    return dt;\n  }\n  /**\n   * Adds the given graph to the existing print preview.\n   *\n   * @param css Optional CSS string to be used in the head section.\n   * @param targetWindow Optional window that should be used for rendering. If\n   * this is specified then no HEAD tag, CSS and BODY tag will be written.\n   */\n  appendGraph(graph, scale, x0, y0, forcePageBreaks, keepOpen) {\n    this.graph = graph;\n    this.scale = scale != null ? scale : 1 / graph.pageScale;\n    this.x0 = x0;\n    this.y0 = y0;\n    this.open(null, null, forcePageBreaks, keepOpen);\n  }\n  /**\n   * Shows the print preview window. The window is created here if it does\n   * not exist.\n   *\n   * @param css Optional CSS string to be used in the head section.\n   * @param targetWindow Optional window that should be used for rendering. If\n   * this is specified then no HEAD tag, CSS and BODY tag will be written.\n   */\n  open() {\n    let css = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : null;\n    let targetWindow = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : null;\n    let forcePageBreaks = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;\n    let keepOpen = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : false;\n    // Closing the window while the page is being rendered may cause an\n    // exception in IE. This and any other exceptions are simply ignored.\n    const previousInitializeOverlay = this.graph.cellRenderer.initializeOverlay;\n    let div = null;\n    try {\n      // Temporarily overrides the method to redirect rendering of overlays\n      // to the draw pane so that they are visible in the printout\n      if (this.printOverlays) {\n        this.graph.cellRenderer.initializeOverlay = (state, overlay) => {\n          overlay.init(state.view.getDrawPane());\n        };\n      }\n      if (this.printControls) {\n        this.graph.cellRenderer.initControl = (state, control, handleEvents, clickHandler) => {\n          control.dialect = state.view.graph.dialect;\n          control.init(state.view.getDrawPane());\n          return null;\n        };\n      }\n      this.wnd = targetWindow != null ? targetWindow : this.wnd;\n      let isNewWindow = false;\n      if (this.wnd == null) {\n        isNewWindow = true;\n        this.wnd = window.open();\n      }\n      if (!this.wnd) {\n        throw new Error('Create new window not allowed');\n      }\n      const doc = this.wnd.document;\n      if (isNewWindow) {\n        const dt = this.getDoctype();\n        if (dt != null && dt.length > 0) {\n          doc.writeln(dt);\n        }\n        if (document.compatMode === 'CSS1Compat') {\n          doc.writeln('<!DOCTYPE html>');\n        }\n        doc.writeln('<html>');\n        doc.writeln('<head>');\n        if (css) {\n          this.writeHead(doc, css);\n        }\n        doc.writeln('</head>');\n        doc.writeln('<body class=\"mxPage\">');\n      }\n      // Computes the horizontal and vertical page count\n      const bounds = this.graph.getGraphBounds().clone();\n      const currentScale = this.graph.getView().getScale();\n      const sc = currentScale / this.scale;\n      const tr = this.graph.getView().getTranslate();\n      // Uses the absolute origin with no offset for all printing\n      if (!this.autoOrigin) {\n        this.x0 -= tr.x * this.scale;\n        this.y0 -= tr.y * this.scale;\n        bounds.width += bounds.x;\n        bounds.height += bounds.y;\n        bounds.x = 0;\n        bounds.y = 0;\n        this.border = 0;\n      }\n      // Store the available page area\n      const availableWidth = this.pageFormat.width - this.border * 2;\n      const availableHeight = this.pageFormat.height - this.border * 2;\n      // Adds margins to page format\n      this.pageFormat.height += this.marginTop + this.marginBottom;\n      // Compute the unscaled, untranslated bounds to find\n      // the number of vertical and horizontal pages\n      bounds.width /= sc;\n      bounds.height /= sc;\n      const hpages = Math.max(1, Math.ceil((bounds.width + this.x0) / availableWidth));\n      const vpages = Math.max(1, Math.ceil((bounds.height + this.y0) / availableHeight));\n      this.pageCount = hpages * vpages;\n      const writePageSelector = () => {\n        if (this.pageSelector && (vpages > 1 || hpages > 1)) {\n          const table = this.createPageSelector(vpages, hpages);\n          doc.body.appendChild(table);\n        }\n      };\n      const addPage = (div, addBreak) => {\n        // Border of the DIV (aka page) inside the document\n        if (this.borderColor != null) {\n          div.style.borderColor = this.borderColor;\n          div.style.borderStyle = 'solid';\n          div.style.borderWidth = '1px';\n        }\n        // Needs to be assigned directly because IE doesn't support\n        // child selectors, eg. body > div { background: white; }\n        div.style.background = this.backgroundColor;\n        if (forcePageBreaks || addBreak) {\n          div.style.pageBreakAfter = 'always';\n        }\n        // NOTE: We are dealing with cross-window DOM here, which\n        // is a problem in IE, so we copy the HTML markup instead.\n        // The underlying problem is that the graph display markup\n        // creation (in mxShape, mxGraphView) is hardwired to using\n        // document.createElement and hence we must use this document\n        // to create the complete page and then copy it over to the\n        // new window.document. This can be fixed later by using the\n        // ownerDocument of the container in mxShape and mxGraphView.\n        if (isNewWindow && Client.IS_EDGE) {\n          // For some obscure reason, removing the DIV from the\n          // parent before fetching its outerHTML has missing\n          // fillcolor properties and fill children, so the div\n          // must be removed afterwards to keep the fillcolors.\n          doc.writeln(div.outerHTML);\n          div.parentNode.removeChild(div);\n        } else if (Client.IS_EDGE) {\n          let clone = doc.createElement('div');\n          clone.innerHTML = div.outerHTML;\n          clone = clone.getElementsByTagName('div')[0];\n          doc.body.appendChild(clone);\n          div.parentNode.removeChild(div);\n        } else {\n          div.parentNode.removeChild(div);\n          doc.body.appendChild(div);\n        }\n        if (forcePageBreaks || addBreak) {\n          this.addPageBreak(doc);\n        }\n      };\n      const cov = this.getCoverPages(this.pageFormat.width, this.pageFormat.height);\n      if (cov != null) {\n        for (let i = 0; i < cov.length; i += 1) {\n          addPage(cov[i], true);\n        }\n      }\n      const apx = this.getAppendices(this.pageFormat.width, this.pageFormat.height);\n      // Appends each page to the page output for printing, making\n      // sure there will be a page break after each page (ie. div)\n      for (let i = 0; i < vpages; i += 1) {\n        const dy = i * availableHeight / this.scale - this.y0 / this.scale + (bounds.y - tr.y * currentScale) / currentScale;\n        for (let j = 0; j < hpages; j++) {\n          if (this.wnd == null) {\n            return null;\n          }\n          const dx = j * availableWidth / this.scale - this.x0 / this.scale + (bounds.x - tr.x * currentScale) / currentScale;\n          const pageNum = i * hpages + j + 1;\n          const clip = new Rectangle(dx, dy, availableWidth, availableHeight);\n          div = this.renderPage(this.pageFormat.width, this.pageFormat.height, 0, 0, div => {\n            this.addGraphFragment(-dx, -dy, this.scale, pageNum, div, clip);\n            if (this.printBackgroundImage) {\n              this.insertBackgroundImage(div, -dx, -dy);\n            }\n          }, pageNum);\n          // Gives the page a unique ID for later accessing the page\n          div.setAttribute('id', \"mxPage-\".concat(pageNum));\n          addPage(div, apx != null || i < vpages - 1 || j < hpages - 1);\n        }\n      }\n      if (apx != null) {\n        for (let i = 0; i < apx.length; i += 1) {\n          addPage(apx[i], i < apx.length - 1);\n        }\n      }\n      if (isNewWindow && !keepOpen) {\n        this.closeDocument();\n        writePageSelector();\n      }\n      this.wnd.focus();\n    } catch (e) {\n      // Removes the DIV from the document in case of an error\n      if (div != null && div.parentNode != null) {\n        div.parentNode.removeChild(div);\n      }\n    } finally {\n      this.graph.cellRenderer.initializeOverlay = previousInitializeOverlay;\n    }\n    return this.wnd;\n  }\n  /**\n   * Adds a page break to the given document.\n   */\n  addPageBreak(doc) {\n    const hr = doc.createElement('hr');\n    hr.className = 'mxPageBreak';\n    doc.body.appendChild(hr);\n  }\n  /**\n   * Writes the closing tags for body and page after calling {@link writePostfix}.\n   */\n  closeDocument() {\n    try {\n      if (this.wnd != null && this.wnd.document != null) {\n        const doc = this.wnd.document;\n        this.writePostfix(doc);\n        doc.writeln('</body>');\n        doc.writeln('</html>');\n        doc.close();\n        // Removes all event handlers in the print output\n        InternalEvent.release(doc.body);\n      }\n    } catch (e) {\n      // ignore any errors resulting from wnd no longer being available\n    }\n  }\n  /**\n   * Writes the HEAD section into the given document, without the opening\n   * and closing HEAD tags.\n   */\n  writeHead(doc, css) {\n    if (this.title != null) {\n      doc.writeln(\"<title>\".concat(this.title, \"</title>\"));\n    }\n    // Adds all required stylesheets\n    addLinkToHead('stylesheet', \"\".concat(Client.basePath, \"/css/common.css\"), doc);\n    // Removes horizontal rules and page selector from print output\n    doc.writeln('<style type=\"text/css\">');\n    doc.writeln('@media print {');\n    doc.writeln('  * { -webkit-print-color-adjust: exact; }');\n    doc.writeln('  table.mxPageSelector { display: none; }');\n    doc.writeln('  hr.mxPageBreak { display: none; }');\n    doc.writeln('}');\n    doc.writeln('@media screen {');\n    // NOTE: position: fixed is not supported in IE, so the page selector\n    // position (absolute) needs to be updated in IE (see below)\n    doc.writeln('  table.mxPageSelector { position: fixed; right: 10px; top: 10px;' + 'font-family: Arial; font-size:10pt; border: solid 1px darkgray;' + 'background: white; border-collapse:collapse; }');\n    doc.writeln('  table.mxPageSelector td { border: solid 1px gray; padding:4px; }');\n    doc.writeln('  body.mxPage { background: gray; }');\n    doc.writeln('}');\n    if (css != null) {\n      doc.writeln(css);\n    }\n    doc.writeln('</style>');\n  }\n  /**\n   * Called before closing the body of the page. This implementation is empty.\n   */\n  writePostfix(doc) {\n    // empty\n  }\n  /**\n   * Creates the page selector table.\n   */\n  createPageSelector(vpages, hpages) {\n    if (!this.wnd) {\n      throw new Error('Popup window not created');\n    }\n    const doc = this.wnd.document;\n    const table = doc.createElement('table');\n    table.className = 'mxPageSelector';\n    table.setAttribute('border', '0');\n    const tbody = doc.createElement('tbody');\n    for (let i = 0; i < vpages; i += 1) {\n      const row = doc.createElement('tr');\n      for (let j = 0; j < hpages; j++) {\n        const pageNum = i * hpages + j + 1;\n        const cell = doc.createElement('td');\n        const a = doc.createElement('a');\n        a.setAttribute('href', \"#mxPage-\".concat(pageNum));\n        // Workaround for FF where the anchor is appended to the URL of the original document\n        if (Client.IS_NS && !Client.IS_SF && !Client.IS_GC) {\n          const js = \"let page = document.getElementById('mxPage-\".concat(pageNum, \"');page.scrollIntoView(true);event.preventDefault();\");\n          a.setAttribute('onclick', js);\n        }\n        write(a, String(pageNum));\n        cell.appendChild(a);\n        row.appendChild(cell);\n      }\n      tbody.appendChild(row);\n    }\n    table.appendChild(tbody);\n    return table;\n  }\n  /**\n   * Creates a DIV that prints a single page of the given\n   * graph using the given scale and returns the DIV that\n   * represents the page.\n   *\n   * @param w Width of the page in pixels.\n   * @param h Height of the page in pixels.\n   * @param dx Optional horizontal page offset in pixels (used internally).\n   * @param dy Optional vertical page offset in pixels (used internally).\n   * @param content Callback that adds the HTML content to the inner div of a page.\n   * Takes the inner div as the argument.\n   * @param pageNumber Integer representing the page number.\n   */\n  renderPage(w, h, dx, dy, content, pageNumber) {\n    let div = document.createElement('div');\n    let arg = null;\n    try {\n      // Workaround for ignored clipping in IE 9 standards\n      // when printing with page breaks and HTML labels.\n      if (dx !== 0 || dy !== 0) {\n        div.style.position = 'relative';\n        div.style.width = \"\".concat(w, \"px\");\n        div.style.height = \"\".concat(h, \"px\");\n        div.style.pageBreakInside = 'avoid';\n        const innerDiv = document.createElement('div');\n        innerDiv.style.position = 'relative';\n        innerDiv.style.top = \"\".concat(this.border, \"px\");\n        innerDiv.style.left = \"\".concat(this.border, \"px\");\n        innerDiv.style.width = \"\".concat(w - 2 * this.border, \"px\");\n        innerDiv.style.height = \"\".concat(h - 2 * this.border, \"px\");\n        innerDiv.style.overflow = 'hidden';\n        const viewport = document.createElement('div');\n        viewport.style.position = 'relative';\n        viewport.style.marginLeft = \"\".concat(dx, \"px\");\n        viewport.style.marginTop = \"\".concat(dy, \"px\");\n        innerDiv.appendChild(viewport);\n        div.appendChild(innerDiv);\n        document.body.appendChild(div);\n        arg = viewport;\n      }\n      // FIXME: IE10/11 too many pages\n      else {\n        div.style.width = \"\".concat(w, \"px\");\n        div.style.height = \"\".concat(h, \"px\");\n        div.style.overflow = 'hidden';\n        div.style.pageBreakInside = 'avoid';\n        const innerDiv = document.createElement('div');\n        innerDiv.style.width = \"\".concat(w - 2 * this.border, \"px\");\n        innerDiv.style.height = \"\".concat(h - 2 * this.border, \"px\");\n        innerDiv.style.overflow = 'hidden';\n        innerDiv.style.top = \"\".concat(this.border, \"px\");\n        innerDiv.style.left = \"\".concat(this.border, \"px\");\n        div.appendChild(innerDiv);\n        document.body.appendChild(div);\n        arg = innerDiv;\n      }\n    } catch (e) {\n      if (div && div.parentNode) {\n        div.parentNode.removeChild(div);\n      }\n      div = null;\n      throw e;\n    }\n    content(arg);\n    return div;\n  }\n  /**\n   * Returns the root cell for painting the graph.\n   */\n  getRoot() {\n    let root = this.graph.view.currentRoot;\n    if (root == null) {\n      root = this.graph.getDataModel().getRoot();\n    }\n    return root;\n  }\n  /**\n   * Returns true if CSS transforms should be used for scaling content.\n   * This returns true if foreignObject is supported and we're not in Safari\n   * as it has clipping bugs for transformed CSS content with foreignObjects.\n   */\n  useCssTransforms() {\n    return !Client.NO_FO && !Client.IS_SF;\n  }\n  /**\n   * Adds a graph fragment to the given div.\n   *\n   * @param dx Horizontal translation for the diagram.\n   * @param dy Vertical translation for the diagram.\n   * @param scale Scale for the diagram.\n   * @param pageNumber Number of the page to be rendered.\n   * @param div Div that contains the output.\n   * @param clip Contains the clipping rectangle as an {@link Rectangle}.\n   */\n  addGraphFragment(dx, dy, scale, pageNumber, div, clip) {\n    const view = this.graph.getView();\n    const previousContainer = this.graph.container;\n    this.graph.container = div;\n    const canvas = view.getCanvas();\n    const backgroundPane = view.getBackgroundPane();\n    const drawPane = view.getDrawPane();\n    const overlayPane = view.getOverlayPane();\n    const realScale = scale;\n    if (this.graph.dialect === DIALECT.SVG) {\n      view.createSvg();\n      // Uses CSS transform for scaling\n      if (this.useCssTransforms()) {\n        const g = view.getDrawPane().parentNode;\n        const prev = g.getAttribute('transform');\n        g.setAttribute('transformOrigin', '0 0');\n        g.setAttribute('transform', \"scale(\".concat(scale, \",\").concat(scale, \")\") + \"translate(\".concat(dx, \",\").concat(dy, \")\"));\n        scale = 1;\n        dx = 0;\n        dy = 0;\n      }\n    } else {\n      view.createHtml();\n    }\n    // Disables events on the view\n    const eventsEnabled = view.isEventsEnabled();\n    view.setEventsEnabled(false);\n    // Disables the graph to avoid cursors\n    const graphEnabled = this.graph.isEnabled();\n    this.graph.setEnabled(false);\n    // Resets the translation\n    const translate = view.getTranslate();\n    view.translate = new Point(dx, dy);\n    // Redraws only states that intersect the clip\n    const {\n      redraw\n    } = this.graph.cellRenderer;\n    const {\n      states\n    } = view;\n    const s = view.scale;\n    // Gets the transformed clip for intersection check below\n    if (this.clipping) {\n      const tempClip = new Rectangle((clip.x + translate.x) * s, (clip.y + translate.y) * s, clip.width * s / realScale, clip.height * s / realScale);\n      // Checks clipping rectangle for speedup\n      // Must create terminal states for edge clipping even if terminal outside of clip\n      this.graph.cellRenderer.redraw = (state, force, rendering) => {\n        if (state != null) {\n          // Gets original state from graph to find bounding box\n          const orig = states.get(state.cell);\n          if (orig != null) {\n            const bbox = view.getBoundingBox(orig, false);\n            // Stops rendering if outside clip for speedup but ignores\n            // edge labels where width and height is set to 0\n            if (bbox != null && bbox.width > 0 && bbox.height > 0 && !intersects(tempClip, bbox)) {\n              return;\n            }\n          }\n        }\n        redraw.apply(this, [state, force, rendering]); // CHECK ME!!!\n      };\n    }\n    let temp = null;\n    try {\n      // Creates the temporary cell states in the view and\n      // draws them onto the temporary DOM nodes in the view\n      const cells = [this.getRoot()];\n      temp = new TemporaryCellStates(view, scale, cells, null, state => {\n        return this.getLinkForCellState(state);\n      });\n    } finally {\n      // Removes everything but the SVG node\n      let tmp = div.firstChild;\n      while (tmp != null) {\n        const next = tmp.nextSibling;\n        const name = tmp.nodeName.toLowerCase();\n        // Note: Width and height are required in FF 11\n        if (name === 'svg') {\n          tmp.style.overflow = 'hidden';\n          tmp.style.position = 'relative';\n          tmp.style.top = \"\".concat(this.marginTop, \"px\");\n          tmp.setAttribute('width', String(clip.width));\n          tmp.setAttribute('height', String(clip.height));\n          tmp.style.width = '';\n          tmp.style.height = '';\n        }\n        // Tries to fetch all text labels and only text labels\n        else if (tmp.style.cursor !== 'default' && name !== 'div') {\n          tmp.parentNode.removeChild(tmp);\n        }\n        tmp = next;\n      }\n      // Puts background image behind SVG output\n      if (this.printBackgroundImage) {\n        const svgs = div.getElementsByTagName('svg');\n        if (svgs.length > 0) {\n          svgs[0].style.position = 'absolute';\n        }\n      }\n      // Completely removes the overlay pane to remove more handles\n      view.overlayPane.parentNode.removeChild(view.overlayPane);\n      // Restores the state of the view\n      this.graph.setEnabled(graphEnabled);\n      this.graph.container = previousContainer;\n      this.graph.cellRenderer.redraw = redraw;\n      view.canvas = canvas;\n      view.backgroundPane = backgroundPane;\n      view.drawPane = drawPane;\n      view.overlayPane = overlayPane;\n      view.translate = translate;\n      if (temp) {\n        temp.destroy();\n      }\n      view.setEventsEnabled(eventsEnabled);\n    }\n  }\n  /**\n   * Returns the link for the given cell state. This returns null.\n   */\n  getLinkForCellState(state) {\n    return this.graph.getLinkForCell(state.cell);\n  }\n  /**\n   * Inserts the background image into the given div.\n   */\n  insertBackgroundImage(div, dx, dy) {\n    const bg = this.graph.backgroundImage;\n    if (bg != null) {\n      const img = document.createElement('img');\n      img.style.position = 'absolute';\n      img.style.marginLeft = \"\".concat(Math.round(dx * this.scale), \"px\");\n      img.style.marginTop = \"\".concat(Math.round(dy * this.scale), \"px\");\n      img.setAttribute('width', String(Math.round(this.scale * bg.width)));\n      img.setAttribute('height', String(Math.round(this.scale * bg.height)));\n      img.src = bg.src;\n      div.insertBefore(img, div.firstChild);\n    }\n  }\n  /**\n   * Returns the pages to be added before the print output. This returns null.\n   */\n  getCoverPages(width, height) {\n    return null;\n  }\n  /**\n   * Returns the pages to be added after the print output. This returns null.\n   */\n  getAppendices(width, height) {\n    return null;\n  }\n  /**\n   * Opens the print preview and shows the print dialog.\n   *\n   * @param css Optional CSS string to be used in the head section.\n   */\n  print(css) {\n    const wnd = this.open(css);\n    if (wnd != null) {\n      wnd.print();\n    }\n  }\n  /**\n   * Closes the print preview window.\n   */\n  close() {\n    if (this.wnd != null) {\n      this.wnd.close();\n      this.wnd = null;\n    }\n  }\n}\nexport default PrintPreview;","map":{"version":3,"names":["Point","Rectangle","TemporaryCellStates","InternalEvent","Client","intersects","DIALECT","addLinkToHead","write","PrintPreview","constructor","graph","scale","arguments","length","undefined","pageFormat","border","x0","y0","borderColor","title","pageSelector","marginTop","marginBottom","autoOrigin","printOverlays","printControls","printBackgroundImage","backgroundColor","wnd","targetWindow","pageCount","clipping","pageScale","fromRectangle","getWindow","getDoctype","dt","appendGraph","forcePageBreaks","keepOpen","open","css","previousInitializeOverlay","cellRenderer","initializeOverlay","div","state","overlay","init","view","getDrawPane","initControl","control","handleEvents","clickHandler","dialect","isNewWindow","window","Error","doc","document","writeln","compatMode","writeHead","bounds","getGraphBounds","clone","currentScale","getView","getScale","sc","tr","getTranslate","x","y","width","height","availableWidth","availableHeight","hpages","Math","max","ceil","vpages","writePageSelector","table","createPageSelector","body","appendChild","addPage","addBreak","style","borderStyle","borderWidth","background","pageBreakAfter","IS_EDGE","outerHTML","parentNode","removeChild","createElement","innerHTML","getElementsByTagName","addPageBreak","cov","getCoverPages","i","apx","getAppendices","dy","j","dx","pageNum","clip","renderPage","addGraphFragment","insertBackgroundImage","setAttribute","concat","closeDocument","focus","e","hr","className","writePostfix","close","release","basePath","tbody","row","cell","a","IS_NS","IS_SF","IS_GC","js","String","w","h","content","pageNumber","arg","position","pageBreakInside","innerDiv","top","left","overflow","viewport","marginLeft","getRoot","root","currentRoot","getDataModel","useCssTransforms","NO_FO","previousContainer","container","canvas","getCanvas","backgroundPane","getBackgroundPane","drawPane","overlayPane","getOverlayPane","realScale","SVG","createSvg","g","prev","getAttribute","createHtml","eventsEnabled","isEventsEnabled","setEventsEnabled","graphEnabled","isEnabled","setEnabled","translate","redraw","states","s","tempClip","force","rendering","orig","get","bbox","getBoundingBox","apply","temp","cells","getLinkForCellState","tmp","firstChild","next","nextSibling","name","nodeName","toLowerCase","cursor","svgs","destroy","getLinkForCell","bg","backgroundImage","img","round","src","insertBefore","print"],"sources":["D:/OSPanel/domains/old/react/react-intro/node_modules/@maxgraph/core/dist/view/other/PrintPreview.js"],"sourcesContent":["/*\nCopyright 2021-present The maxGraph project Contributors\nCopyright (c) 2006-2019, JGraph Ltd\nCopyright (c) 2006-2017, draw.io AG\n\nLicensed under the Apache License, Version 2.0 (the \"License\");\nyou may not use this file except in compliance with the License.\nYou may obtain a copy of the License at\n\n    http://www.apache.org/licenses/LICENSE-2.0\n\nUnless required by applicable law or agreed to in writing, software\ndistributed under the License is distributed on an \"AS IS\" BASIS,\nWITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.\nSee the License for the specific language governing permissions and\nlimitations under the License.\n*/\nimport Point from '../geometry/Point';\nimport Rectangle from '../geometry/Rectangle';\nimport TemporaryCellStates from '../cell/TemporaryCellStates';\nimport InternalEvent from '../event/InternalEvent';\nimport Client from '../../Client';\nimport { intersects } from '../../util/mathUtils';\nimport { DIALECT } from '../../util/Constants';\nimport { addLinkToHead, write } from '../../util/domUtils';\n/**\n * @class PrintPreview\n *\n * Implements printing of a diagram across multiple pages. The following opens\n * a print preview for an existing graph:\n *\n * ```javascript\n * var preview = new mxPrintPreview(graph);\n * preview.open();\n * ```\n *\n * Use {@link getScaleForPageCount} as follows in order to print the graph\n * across a given number of pages:\n *\n * ```javascript\n * var pageCount = mxUtils.prompt('Enter page count', '1');\n *\n * if (pageCount != null)\n * {\n *   var scale = mxUtils.getScaleForPageCount(pageCount, graph);\n *   var preview = new mxPrintPreview(graph, scale);\n *   preview.open();\n * }\n * ```\n *\n * ### Additional pages\n *\n * To add additional pages before and after the output, {@link getCoverPages} and\n * {@link getAppendices} can be used, respectively.\n *\n * ```javascript\n * var preview = new mxPrintPreview(graph, 1);\n *\n * preview.getCoverPages(w, h)\n * {\n *   return [this.renderPage(w, h, 0, 0, mxUtils.bind(this, function(div)\n *   {\n *     div.innerHTML = '<div style=\"position:relative;margin:4px;\">Cover Page</p>'\n *   }))];\n * };\n *\n * preview.getAppendices(w, h)\n * {\n *   return [this.renderPage(w, h, 0, 0, mxUtils.bind(this, function(div)\n *   {\n *     div.innerHTML = '<div style=\"position:relative;margin:4px;\">Appendix</p>'\n *   }))];\n * };\n *\n * preview.open();\n * ```\n *\n * ### CSS\n *\n * The CSS from the original page is not carried over to the print preview.\n * To add CSS to the page, use the css argument in the {@link open} function or\n * override {@link writeHead} to add the respective link tags as follows:\n *\n * ```javascript\n * var writeHead = preview.writeHead;\n * preview.writeHead(doc, css)\n * {\n *   writeHead.apply(this, arguments);\n *   doc.writeln('<link rel=\"stylesheet\" type=\"text/css\" href=\"style.css\">');\n * };\n * ```\n *\n * ### Padding\n *\n * To add a padding to the page in the preview (but not the print output), use\n * the following code:\n *\n * ```javascript\n * preview.writeHead(doc)\n * {\n *   writeHead.apply(this, arguments);\n *\n *   doc.writeln('<style type=\"text/css\">');\n *   doc.writeln('@media screen {');\n *   doc.writeln('  body > div { padding-top:30px;padding-left:40px;box-sizing:content-box; }');\n *   doc.writeln('}');\n *   doc.writeln('</style>');\n * };\n * ```\n *\n * ### Headers\n *\n * Apart from setting the title argument in the mxPrintPreview constructor you\n * can override {@link renderPage} as follows to add a header to any page:\n *\n * ```javascript\n * var oldRenderPage = renderPage;\n * renderPage(w, h, x, y, content, pageNumber)\n * {\n *   var div = oldRenderPage.apply(this, arguments);\n *\n *   var header = document.createElement('div');\n *   header.style.position = 'absolute';\n *   header.style.top = '0px';\n *   header.style.width = '100%';\n *   header.style.textAlign = 'right';\n *   mxUtils.write(header, 'Your header here');\n *   div.firstChild.appendChild(header);\n *\n *   return div;\n * };\n * ```\n *\n * The pageNumber argument contains the number of the current page, starting at\n * 1. To display a header on the first page only, check pageNumber and add a\n * vertical offset in the constructor call for the height of the header.\n *\n * ### Page Format\n *\n * For landscape printing, use {@link mxConstants.PAGE_FORMAT_A4_LANDSCAPE} as\n * the pageFormat in {@link getScaleForPageCount} and {@link PrintPreview}.\n * Keep in mind that one can not set the defaults for the print dialog\n * of the operating system from JavaScript so the user must manually choose\n * a page format that matches this setting.\n *\n * You can try passing the following CSS directive to {@link open} to set the\n * page format in the print dialog to landscape. However, this CSS\n * directive seems to be ignored in most major browsers, including IE.\n *\n * ```javascript\n * @page {\n *   size: landscape;\n * }\n * ```\n *\n * Note that the print preview behaves differently in IE when used from the\n * filesystem or via HTTP so printing should always be tested via HTTP.\n *\n * If you are using a DOCTYPE in the source page you can override {@link getDoctype}\n * and provide the same DOCTYPE for the print preview if required. Here is\n * an example for IE8 standards mode.\n *\n * ```javascript\n * var preview = new mxPrintPreview(graph);\n * preview.getDoctype()\n * {\n *   return '<!--[if IE]><meta http-equiv=\"X-UA-Compatible\" content=\"IE=5,IE=8\" ><![endif]-->';\n * };\n * preview.open();\n * ```\n */\nclass PrintPreview {\n    constructor(graph, scale = null, pageFormat = null, border = null, x0 = 0, y0 = 0, borderColor = null, title = 'Printer-friendly version', pageSelector = null) {\n        /**\n         * The border inset around each side of every page in the preview. This is set\n         * to 0 if autoOrigin is false.\n         * @default 0\n         */\n        this.border = 0;\n        /**\n         * The margin at the top of the page (number).\n         * @default 0\n         */\n        this.marginTop = 0;\n        /**\n         * The margin at the bottom of the page (number).\n         * @default 0\n         */\n        this.marginBottom = 0;\n        /**\n         * Holds the horizontal offset of the output.\n         */\n        this.x0 = 0;\n        /**\n         * Holds the vertical offset of the output.\n         */\n        this.y0 = 0;\n        /**\n         * Specifies if the origin should be automatically computed based on the top,\n         * left corner of the actual diagram contents. The required offset will be added\n         * to {@link x0} and {@link y0} in {@link open}.\n         * @default true\n         */\n        this.autoOrigin = true;\n        /**\n         * Specifies if overlays should be printed.\n         * @default false\n         */\n        this.printOverlays = false;\n        /**\n         * Specifies if controls (such as folding icons) should be printed. Default is\n         * false.\n         */\n        this.printControls = false;\n        /**\n         * Specifies if the background image should be printed.\n         * @default false\n         */\n        this.printBackgroundImage = false;\n        /**\n         * Holds the color value for the page background color.\n         * @default '#ffffff'\n         */\n        this.backgroundColor = '#ffffff';\n        /**\n         * Reference to the preview window.\n         */\n        this.wnd = null;\n        /**\n         * Assign any window here to redirect the rendering in {@link open}.\n         */\n        this.targetWindow = null;\n        /**\n         * Holds the actual number of pages in the preview.\n         */\n        this.pageCount = 0;\n        /**\n         * Specifies is clipping should be used to avoid creating too many cell states\n         * in large diagrams. The bounding box of the cells in the original diagram is\n         * used if this is enabled.\n         * @default true\n         */\n        this.clipping = true;\n        this.graph = graph;\n        this.scale = scale != null ? scale : 1 / graph.pageScale;\n        this.border = border != null ? border : 0;\n        this.pageFormat = Rectangle.fromRectangle(pageFormat != null ? pageFormat : graph.pageFormat);\n        this.title = title;\n        this.x0 = x0;\n        this.y0 = y0;\n        this.borderColor = borderColor;\n        this.pageSelector = pageSelector != null ? pageSelector : true;\n    }\n    /**\n     * Returns {@link wnd}.\n     */\n    getWindow() {\n        return this.wnd;\n    }\n    /**\n     * Returns the string that should go before the HTML tag in the print preview\n     * page. This implementation returns an X-UA meta tag for IE5 in quirks mode,\n     * IE8 in IE8 standards mode and edge in IE9 standards mode.\n     */\n    getDoctype() {\n        const dt = '';\n        return dt;\n    }\n    /**\n     * Adds the given graph to the existing print preview.\n     *\n     * @param css Optional CSS string to be used in the head section.\n     * @param targetWindow Optional window that should be used for rendering. If\n     * this is specified then no HEAD tag, CSS and BODY tag will be written.\n     */\n    appendGraph(graph, scale, x0, y0, forcePageBreaks, keepOpen) {\n        this.graph = graph;\n        this.scale = scale != null ? scale : 1 / graph.pageScale;\n        this.x0 = x0;\n        this.y0 = y0;\n        this.open(null, null, forcePageBreaks, keepOpen);\n    }\n    /**\n     * Shows the print preview window. The window is created here if it does\n     * not exist.\n     *\n     * @param css Optional CSS string to be used in the head section.\n     * @param targetWindow Optional window that should be used for rendering. If\n     * this is specified then no HEAD tag, CSS and BODY tag will be written.\n     */\n    open(css = null, targetWindow = null, forcePageBreaks = false, keepOpen = false) {\n        // Closing the window while the page is being rendered may cause an\n        // exception in IE. This and any other exceptions are simply ignored.\n        const previousInitializeOverlay = this.graph.cellRenderer.initializeOverlay;\n        let div = null;\n        try {\n            // Temporarily overrides the method to redirect rendering of overlays\n            // to the draw pane so that they are visible in the printout\n            if (this.printOverlays) {\n                this.graph.cellRenderer.initializeOverlay = (state, overlay) => {\n                    overlay.init(state.view.getDrawPane());\n                };\n            }\n            if (this.printControls) {\n                this.graph.cellRenderer.initControl = (state, control, handleEvents, clickHandler) => {\n                    control.dialect = state.view.graph.dialect;\n                    control.init(state.view.getDrawPane());\n                    return null;\n                };\n            }\n            this.wnd = targetWindow != null ? targetWindow : this.wnd;\n            let isNewWindow = false;\n            if (this.wnd == null) {\n                isNewWindow = true;\n                this.wnd = window.open();\n            }\n            if (!this.wnd) {\n                throw new Error('Create new window not allowed');\n            }\n            const doc = this.wnd.document;\n            if (isNewWindow) {\n                const dt = this.getDoctype();\n                if (dt != null && dt.length > 0) {\n                    doc.writeln(dt);\n                }\n                if (document.compatMode === 'CSS1Compat') {\n                    doc.writeln('<!DOCTYPE html>');\n                }\n                doc.writeln('<html>');\n                doc.writeln('<head>');\n                if (css) {\n                    this.writeHead(doc, css);\n                }\n                doc.writeln('</head>');\n                doc.writeln('<body class=\"mxPage\">');\n            }\n            // Computes the horizontal and vertical page count\n            const bounds = this.graph.getGraphBounds().clone();\n            const currentScale = this.graph.getView().getScale();\n            const sc = currentScale / this.scale;\n            const tr = this.graph.getView().getTranslate();\n            // Uses the absolute origin with no offset for all printing\n            if (!this.autoOrigin) {\n                this.x0 -= tr.x * this.scale;\n                this.y0 -= tr.y * this.scale;\n                bounds.width += bounds.x;\n                bounds.height += bounds.y;\n                bounds.x = 0;\n                bounds.y = 0;\n                this.border = 0;\n            }\n            // Store the available page area\n            const availableWidth = this.pageFormat.width - this.border * 2;\n            const availableHeight = this.pageFormat.height - this.border * 2;\n            // Adds margins to page format\n            this.pageFormat.height += this.marginTop + this.marginBottom;\n            // Compute the unscaled, untranslated bounds to find\n            // the number of vertical and horizontal pages\n            bounds.width /= sc;\n            bounds.height /= sc;\n            const hpages = Math.max(1, Math.ceil((bounds.width + this.x0) / availableWidth));\n            const vpages = Math.max(1, Math.ceil((bounds.height + this.y0) / availableHeight));\n            this.pageCount = hpages * vpages;\n            const writePageSelector = () => {\n                if (this.pageSelector && (vpages > 1 || hpages > 1)) {\n                    const table = this.createPageSelector(vpages, hpages);\n                    doc.body.appendChild(table);\n                }\n            };\n            const addPage = (div, addBreak) => {\n                // Border of the DIV (aka page) inside the document\n                if (this.borderColor != null) {\n                    div.style.borderColor = this.borderColor;\n                    div.style.borderStyle = 'solid';\n                    div.style.borderWidth = '1px';\n                }\n                // Needs to be assigned directly because IE doesn't support\n                // child selectors, eg. body > div { background: white; }\n                div.style.background = this.backgroundColor;\n                if (forcePageBreaks || addBreak) {\n                    div.style.pageBreakAfter = 'always';\n                }\n                // NOTE: We are dealing with cross-window DOM here, which\n                // is a problem in IE, so we copy the HTML markup instead.\n                // The underlying problem is that the graph display markup\n                // creation (in mxShape, mxGraphView) is hardwired to using\n                // document.createElement and hence we must use this document\n                // to create the complete page and then copy it over to the\n                // new window.document. This can be fixed later by using the\n                // ownerDocument of the container in mxShape and mxGraphView.\n                if (isNewWindow && Client.IS_EDGE) {\n                    // For some obscure reason, removing the DIV from the\n                    // parent before fetching its outerHTML has missing\n                    // fillcolor properties and fill children, so the div\n                    // must be removed afterwards to keep the fillcolors.\n                    doc.writeln(div.outerHTML);\n                    div.parentNode.removeChild(div);\n                }\n                else if (Client.IS_EDGE) {\n                    let clone = doc.createElement('div');\n                    clone.innerHTML = div.outerHTML;\n                    clone = clone.getElementsByTagName('div')[0];\n                    doc.body.appendChild(clone);\n                    div.parentNode.removeChild(div);\n                }\n                else {\n                    div.parentNode.removeChild(div);\n                    doc.body.appendChild(div);\n                }\n                if (forcePageBreaks || addBreak) {\n                    this.addPageBreak(doc);\n                }\n            };\n            const cov = this.getCoverPages(this.pageFormat.width, this.pageFormat.height);\n            if (cov != null) {\n                for (let i = 0; i < cov.length; i += 1) {\n                    addPage(cov[i], true);\n                }\n            }\n            const apx = this.getAppendices(this.pageFormat.width, this.pageFormat.height);\n            // Appends each page to the page output for printing, making\n            // sure there will be a page break after each page (ie. div)\n            for (let i = 0; i < vpages; i += 1) {\n                const dy = (i * availableHeight) / this.scale -\n                    this.y0 / this.scale +\n                    (bounds.y - tr.y * currentScale) / currentScale;\n                for (let j = 0; j < hpages; j++) {\n                    if (this.wnd == null) {\n                        return null;\n                    }\n                    const dx = (j * availableWidth) / this.scale -\n                        this.x0 / this.scale +\n                        (bounds.x - tr.x * currentScale) / currentScale;\n                    const pageNum = i * hpages + j + 1;\n                    const clip = new Rectangle(dx, dy, availableWidth, availableHeight);\n                    div = this.renderPage(this.pageFormat.width, this.pageFormat.height, 0, 0, (div) => {\n                        this.addGraphFragment(-dx, -dy, this.scale, pageNum, div, clip);\n                        if (this.printBackgroundImage) {\n                            this.insertBackgroundImage(div, -dx, -dy);\n                        }\n                    }, pageNum);\n                    // Gives the page a unique ID for later accessing the page\n                    div.setAttribute('id', `mxPage-${pageNum}`);\n                    addPage(div, apx != null || i < vpages - 1 || j < hpages - 1);\n                }\n            }\n            if (apx != null) {\n                for (let i = 0; i < apx.length; i += 1) {\n                    addPage(apx[i], i < apx.length - 1);\n                }\n            }\n            if (isNewWindow && !keepOpen) {\n                this.closeDocument();\n                writePageSelector();\n            }\n            this.wnd.focus();\n        }\n        catch (e) {\n            // Removes the DIV from the document in case of an error\n            if (div != null && div.parentNode != null) {\n                div.parentNode.removeChild(div);\n            }\n        }\n        finally {\n            this.graph.cellRenderer.initializeOverlay = previousInitializeOverlay;\n        }\n        return this.wnd;\n    }\n    /**\n     * Adds a page break to the given document.\n     */\n    addPageBreak(doc) {\n        const hr = doc.createElement('hr');\n        hr.className = 'mxPageBreak';\n        doc.body.appendChild(hr);\n    }\n    /**\n     * Writes the closing tags for body and page after calling {@link writePostfix}.\n     */\n    closeDocument() {\n        try {\n            if (this.wnd != null && this.wnd.document != null) {\n                const doc = this.wnd.document;\n                this.writePostfix(doc);\n                doc.writeln('</body>');\n                doc.writeln('</html>');\n                doc.close();\n                // Removes all event handlers in the print output\n                InternalEvent.release(doc.body);\n            }\n        }\n        catch (e) {\n            // ignore any errors resulting from wnd no longer being available\n        }\n    }\n    /**\n     * Writes the HEAD section into the given document, without the opening\n     * and closing HEAD tags.\n     */\n    writeHead(doc, css) {\n        if (this.title != null) {\n            doc.writeln(`<title>${this.title}</title>`);\n        }\n        // Adds all required stylesheets\n        addLinkToHead('stylesheet', `${Client.basePath}/css/common.css`, doc);\n        // Removes horizontal rules and page selector from print output\n        doc.writeln('<style type=\"text/css\">');\n        doc.writeln('@media print {');\n        doc.writeln('  * { -webkit-print-color-adjust: exact; }');\n        doc.writeln('  table.mxPageSelector { display: none; }');\n        doc.writeln('  hr.mxPageBreak { display: none; }');\n        doc.writeln('}');\n        doc.writeln('@media screen {');\n        // NOTE: position: fixed is not supported in IE, so the page selector\n        // position (absolute) needs to be updated in IE (see below)\n        doc.writeln('  table.mxPageSelector { position: fixed; right: 10px; top: 10px;' +\n            'font-family: Arial; font-size:10pt; border: solid 1px darkgray;' +\n            'background: white; border-collapse:collapse; }');\n        doc.writeln('  table.mxPageSelector td { border: solid 1px gray; padding:4px; }');\n        doc.writeln('  body.mxPage { background: gray; }');\n        doc.writeln('}');\n        if (css != null) {\n            doc.writeln(css);\n        }\n        doc.writeln('</style>');\n    }\n    /**\n     * Called before closing the body of the page. This implementation is empty.\n     */\n    writePostfix(doc) {\n        // empty\n    }\n    /**\n     * Creates the page selector table.\n     */\n    createPageSelector(vpages, hpages) {\n        if (!this.wnd) {\n            throw new Error('Popup window not created');\n        }\n        const doc = this.wnd.document;\n        const table = doc.createElement('table');\n        table.className = 'mxPageSelector';\n        table.setAttribute('border', '0');\n        const tbody = doc.createElement('tbody');\n        for (let i = 0; i < vpages; i += 1) {\n            const row = doc.createElement('tr');\n            for (let j = 0; j < hpages; j++) {\n                const pageNum = i * hpages + j + 1;\n                const cell = doc.createElement('td');\n                const a = doc.createElement('a');\n                a.setAttribute('href', `#mxPage-${pageNum}`);\n                // Workaround for FF where the anchor is appended to the URL of the original document\n                if (Client.IS_NS && !Client.IS_SF && !Client.IS_GC) {\n                    const js = `let page = document.getElementById('mxPage-${pageNum}');page.scrollIntoView(true);event.preventDefault();`;\n                    a.setAttribute('onclick', js);\n                }\n                write(a, String(pageNum));\n                cell.appendChild(a);\n                row.appendChild(cell);\n            }\n            tbody.appendChild(row);\n        }\n        table.appendChild(tbody);\n        return table;\n    }\n    /**\n     * Creates a DIV that prints a single page of the given\n     * graph using the given scale and returns the DIV that\n     * represents the page.\n     *\n     * @param w Width of the page in pixels.\n     * @param h Height of the page in pixels.\n     * @param dx Optional horizontal page offset in pixels (used internally).\n     * @param dy Optional vertical page offset in pixels (used internally).\n     * @param content Callback that adds the HTML content to the inner div of a page.\n     * Takes the inner div as the argument.\n     * @param pageNumber Integer representing the page number.\n     */\n    renderPage(w, h, dx, dy, content, pageNumber) {\n        let div = document.createElement('div');\n        let arg = null;\n        try {\n            // Workaround for ignored clipping in IE 9 standards\n            // when printing with page breaks and HTML labels.\n            if (dx !== 0 || dy !== 0) {\n                div.style.position = 'relative';\n                div.style.width = `${w}px`;\n                div.style.height = `${h}px`;\n                div.style.pageBreakInside = 'avoid';\n                const innerDiv = document.createElement('div');\n                innerDiv.style.position = 'relative';\n                innerDiv.style.top = `${this.border}px`;\n                innerDiv.style.left = `${this.border}px`;\n                innerDiv.style.width = `${w - 2 * this.border}px`;\n                innerDiv.style.height = `${h - 2 * this.border}px`;\n                innerDiv.style.overflow = 'hidden';\n                const viewport = document.createElement('div');\n                viewport.style.position = 'relative';\n                viewport.style.marginLeft = `${dx}px`;\n                viewport.style.marginTop = `${dy}px`;\n                innerDiv.appendChild(viewport);\n                div.appendChild(innerDiv);\n                document.body.appendChild(div);\n                arg = viewport;\n            }\n            // FIXME: IE10/11 too many pages\n            else {\n                div.style.width = `${w}px`;\n                div.style.height = `${h}px`;\n                div.style.overflow = 'hidden';\n                div.style.pageBreakInside = 'avoid';\n                const innerDiv = document.createElement('div');\n                innerDiv.style.width = `${w - 2 * this.border}px`;\n                innerDiv.style.height = `${h - 2 * this.border}px`;\n                innerDiv.style.overflow = 'hidden';\n                innerDiv.style.top = `${this.border}px`;\n                innerDiv.style.left = `${this.border}px`;\n                div.appendChild(innerDiv);\n                document.body.appendChild(div);\n                arg = innerDiv;\n            }\n        }\n        catch (e) {\n            if (div && div.parentNode) {\n                div.parentNode.removeChild(div);\n            }\n            div = null;\n            throw e;\n        }\n        content(arg);\n        return div;\n    }\n    /**\n     * Returns the root cell for painting the graph.\n     */\n    getRoot() {\n        let root = this.graph.view.currentRoot;\n        if (root == null) {\n            root = this.graph.getDataModel().getRoot();\n        }\n        return root;\n    }\n    /**\n     * Returns true if CSS transforms should be used for scaling content.\n     * This returns true if foreignObject is supported and we're not in Safari\n     * as it has clipping bugs for transformed CSS content with foreignObjects.\n     */\n    useCssTransforms() {\n        return !Client.NO_FO && !Client.IS_SF;\n    }\n    /**\n     * Adds a graph fragment to the given div.\n     *\n     * @param dx Horizontal translation for the diagram.\n     * @param dy Vertical translation for the diagram.\n     * @param scale Scale for the diagram.\n     * @param pageNumber Number of the page to be rendered.\n     * @param div Div that contains the output.\n     * @param clip Contains the clipping rectangle as an {@link Rectangle}.\n     */\n    addGraphFragment(dx, dy, scale, pageNumber, div, clip) {\n        const view = this.graph.getView();\n        const previousContainer = this.graph.container;\n        this.graph.container = div;\n        const canvas = view.getCanvas();\n        const backgroundPane = view.getBackgroundPane();\n        const drawPane = view.getDrawPane();\n        const overlayPane = view.getOverlayPane();\n        const realScale = scale;\n        if (this.graph.dialect === DIALECT.SVG) {\n            view.createSvg();\n            // Uses CSS transform for scaling\n            if (this.useCssTransforms()) {\n                const g = view.getDrawPane().parentNode;\n                const prev = g.getAttribute('transform');\n                g.setAttribute('transformOrigin', '0 0');\n                g.setAttribute('transform', `scale(${scale},${scale})` + `translate(${dx},${dy})`);\n                scale = 1;\n                dx = 0;\n                dy = 0;\n            }\n        }\n        else {\n            view.createHtml();\n        }\n        // Disables events on the view\n        const eventsEnabled = view.isEventsEnabled();\n        view.setEventsEnabled(false);\n        // Disables the graph to avoid cursors\n        const graphEnabled = this.graph.isEnabled();\n        this.graph.setEnabled(false);\n        // Resets the translation\n        const translate = view.getTranslate();\n        view.translate = new Point(dx, dy);\n        // Redraws only states that intersect the clip\n        const { redraw } = this.graph.cellRenderer;\n        const { states } = view;\n        const s = view.scale;\n        // Gets the transformed clip for intersection check below\n        if (this.clipping) {\n            const tempClip = new Rectangle((clip.x + translate.x) * s, (clip.y + translate.y) * s, (clip.width * s) / realScale, (clip.height * s) / realScale);\n            // Checks clipping rectangle for speedup\n            // Must create terminal states for edge clipping even if terminal outside of clip\n            this.graph.cellRenderer.redraw = (state, force, rendering) => {\n                if (state != null) {\n                    // Gets original state from graph to find bounding box\n                    const orig = states.get(state.cell);\n                    if (orig != null) {\n                        const bbox = view.getBoundingBox(orig, false);\n                        // Stops rendering if outside clip for speedup but ignores\n                        // edge labels where width and height is set to 0\n                        if (bbox != null &&\n                            bbox.width > 0 &&\n                            bbox.height > 0 &&\n                            !intersects(tempClip, bbox)) {\n                            return;\n                        }\n                    }\n                }\n                redraw.apply(this, [state, force, rendering]); // CHECK ME!!!\n            };\n        }\n        let temp = null;\n        try {\n            // Creates the temporary cell states in the view and\n            // draws them onto the temporary DOM nodes in the view\n            const cells = [this.getRoot()];\n            temp = new TemporaryCellStates(view, scale, cells, null, (state) => {\n                return this.getLinkForCellState(state);\n            });\n        }\n        finally {\n            // Removes everything but the SVG node\n            let tmp = div.firstChild;\n            while (tmp != null) {\n                const next = tmp.nextSibling;\n                const name = tmp.nodeName.toLowerCase();\n                // Note: Width and height are required in FF 11\n                if (name === 'svg') {\n                    tmp.style.overflow = 'hidden';\n                    tmp.style.position = 'relative';\n                    tmp.style.top = `${this.marginTop}px`;\n                    tmp.setAttribute('width', String(clip.width));\n                    tmp.setAttribute('height', String(clip.height));\n                    tmp.style.width = '';\n                    tmp.style.height = '';\n                }\n                // Tries to fetch all text labels and only text labels\n                else if (tmp.style.cursor !== 'default' && name !== 'div') {\n                    tmp.parentNode.removeChild(tmp);\n                }\n                tmp = next;\n            }\n            // Puts background image behind SVG output\n            if (this.printBackgroundImage) {\n                const svgs = div.getElementsByTagName('svg');\n                if (svgs.length > 0) {\n                    svgs[0].style.position = 'absolute';\n                }\n            }\n            // Completely removes the overlay pane to remove more handles\n            view.overlayPane.parentNode.removeChild(view.overlayPane);\n            // Restores the state of the view\n            this.graph.setEnabled(graphEnabled);\n            this.graph.container = previousContainer;\n            this.graph.cellRenderer.redraw = redraw;\n            view.canvas = canvas;\n            view.backgroundPane = backgroundPane;\n            view.drawPane = drawPane;\n            view.overlayPane = overlayPane;\n            view.translate = translate;\n            if (temp) {\n                temp.destroy();\n            }\n            view.setEventsEnabled(eventsEnabled);\n        }\n    }\n    /**\n     * Returns the link for the given cell state. This returns null.\n     */\n    getLinkForCellState(state) {\n        return this.graph.getLinkForCell(state.cell);\n    }\n    /**\n     * Inserts the background image into the given div.\n     */\n    insertBackgroundImage(div, dx, dy) {\n        const bg = this.graph.backgroundImage;\n        if (bg != null) {\n            const img = document.createElement('img');\n            img.style.position = 'absolute';\n            img.style.marginLeft = `${Math.round(dx * this.scale)}px`;\n            img.style.marginTop = `${Math.round(dy * this.scale)}px`;\n            img.setAttribute('width', String(Math.round(this.scale * bg.width)));\n            img.setAttribute('height', String(Math.round(this.scale * bg.height)));\n            img.src = bg.src;\n            div.insertBefore(img, div.firstChild);\n        }\n    }\n    /**\n     * Returns the pages to be added before the print output. This returns null.\n     */\n    getCoverPages(width, height) {\n        return null;\n    }\n    /**\n     * Returns the pages to be added after the print output. This returns null.\n     */\n    getAppendices(width, height) {\n        return null;\n    }\n    /**\n     * Opens the print preview and shows the print dialog.\n     *\n     * @param css Optional CSS string to be used in the head section.\n     */\n    print(css) {\n        const wnd = this.open(css);\n        if (wnd != null) {\n            wnd.print();\n        }\n    }\n    /**\n     * Closes the print preview window.\n     */\n    close() {\n        if (this.wnd != null) {\n            this.wnd.close();\n            this.wnd = null;\n        }\n    }\n}\nexport default PrintPreview;\n"],"mappings":"AAAA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,OAAOA,KAAK,MAAM,mBAAmB;AACrC,OAAOC,SAAS,MAAM,uBAAuB;AAC7C,OAAOC,mBAAmB,MAAM,6BAA6B;AAC7D,OAAOC,aAAa,MAAM,wBAAwB;AAClD,OAAOC,MAAM,MAAM,cAAc;AACjC,SAASC,UAAU,QAAQ,sBAAsB;AACjD,SAASC,OAAO,QAAQ,sBAAsB;AAC9C,SAASC,aAAa,EAAEC,KAAK,QAAQ,qBAAqB;AAC1D;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA,MAAMC,YAAY,CAAC;EACfC,WAAWA,CAACC,KAAK,EAA+I;IAAA,IAA7IC,KAAK,GAAAC,SAAA,CAAAC,MAAA,QAAAD,SAAA,QAAAE,SAAA,GAAAF,SAAA,MAAG,IAAI;IAAA,IAAEG,UAAU,GAAAH,SAAA,CAAAC,MAAA,QAAAD,SAAA,QAAAE,SAAA,GAAAF,SAAA,MAAG,IAAI;IAAA,IAAEI,MAAM,GAAAJ,SAAA,CAAAC,MAAA,QAAAD,SAAA,QAAAE,SAAA,GAAAF,SAAA,MAAG,IAAI;IAAA,IAAEK,EAAE,GAAAL,SAAA,CAAAC,MAAA,QAAAD,SAAA,QAAAE,SAAA,GAAAF,SAAA,MAAG,CAAC;IAAA,IAAEM,EAAE,GAAAN,SAAA,CAAAC,MAAA,QAAAD,SAAA,QAAAE,SAAA,GAAAF,SAAA,MAAG,CAAC;IAAA,IAAEO,WAAW,GAAAP,SAAA,CAAAC,MAAA,QAAAD,SAAA,QAAAE,SAAA,GAAAF,SAAA,MAAG,IAAI;IAAA,IAAEQ,KAAK,GAAAR,SAAA,CAAAC,MAAA,QAAAD,SAAA,QAAAE,SAAA,GAAAF,SAAA,MAAG,0BAA0B;IAAA,IAAES,YAAY,GAAAT,SAAA,CAAAC,MAAA,QAAAD,SAAA,QAAAE,SAAA,GAAAF,SAAA,MAAG,IAAI;IAC1J;AACR;AACA;AACA;AACA;IACQ,IAAI,CAACI,MAAM,GAAG,CAAC;IACf;AACR;AACA;AACA;IACQ,IAAI,CAACM,SAAS,GAAG,CAAC;IAClB;AACR;AACA;AACA;IACQ,IAAI,CAACC,YAAY,GAAG,CAAC;IACrB;AACR;AACA;IACQ,IAAI,CAACN,EAAE,GAAG,CAAC;IACX;AACR;AACA;IACQ,IAAI,CAACC,EAAE,GAAG,CAAC;IACX;AACR;AACA;AACA;AACA;AACA;IACQ,IAAI,CAACM,UAAU,GAAG,IAAI;IACtB;AACR;AACA;AACA;IACQ,IAAI,CAACC,aAAa,GAAG,KAAK;IAC1B;AACR;AACA;AACA;IACQ,IAAI,CAACC,aAAa,GAAG,KAAK;IAC1B;AACR;AACA;AACA;IACQ,IAAI,CAACC,oBAAoB,GAAG,KAAK;IACjC;AACR;AACA;AACA;IACQ,IAAI,CAACC,eAAe,GAAG,SAAS;IAChC;AACR;AACA;IACQ,IAAI,CAACC,GAAG,GAAG,IAAI;IACf;AACR;AACA;IACQ,IAAI,CAACC,YAAY,GAAG,IAAI;IACxB;AACR;AACA;IACQ,IAAI,CAACC,SAAS,GAAG,CAAC;IAClB;AACR;AACA;AACA;AACA;AACA;IACQ,IAAI,CAACC,QAAQ,GAAG,IAAI;IACpB,IAAI,CAACtB,KAAK,GAAGA,KAAK;IAClB,IAAI,CAACC,KAAK,GAAGA,KAAK,IAAI,IAAI,GAAGA,KAAK,GAAG,CAAC,GAAGD,KAAK,CAACuB,SAAS;IACxD,IAAI,CAACjB,MAAM,GAAGA,MAAM,IAAI,IAAI,GAAGA,MAAM,GAAG,CAAC;IACzC,IAAI,CAACD,UAAU,GAAGf,SAAS,CAACkC,aAAa,CAACnB,UAAU,IAAI,IAAI,GAAGA,UAAU,GAAGL,KAAK,CAACK,UAAU,CAAC;IAC7F,IAAI,CAACK,KAAK,GAAGA,KAAK;IAClB,IAAI,CAACH,EAAE,GAAGA,EAAE;IACZ,IAAI,CAACC,EAAE,GAAGA,EAAE;IACZ,IAAI,CAACC,WAAW,GAAGA,WAAW;IAC9B,IAAI,CAACE,YAAY,GAAGA,YAAY,IAAI,IAAI,GAAGA,YAAY,GAAG,IAAI;EAClE;EACA;AACJ;AACA;EACIc,SAASA,CAAA,EAAG;IACR,OAAO,IAAI,CAACN,GAAG;EACnB;EACA;AACJ;AACA;AACA;AACA;EACIO,UAAUA,CAAA,EAAG;IACT,MAAMC,EAAE,GAAG,EAAE;IACb,OAAOA,EAAE;EACb;EACA;AACJ;AACA;AACA;AACA;AACA;AACA;EACIC,WAAWA,CAAC5B,KAAK,EAAEC,KAAK,EAAEM,EAAE,EAAEC,EAAE,EAAEqB,eAAe,EAAEC,QAAQ,EAAE;IACzD,IAAI,CAAC9B,KAAK,GAAGA,KAAK;IAClB,IAAI,CAACC,KAAK,GAAGA,KAAK,IAAI,IAAI,GAAGA,KAAK,GAAG,CAAC,GAAGD,KAAK,CAACuB,SAAS;IACxD,IAAI,CAAChB,EAAE,GAAGA,EAAE;IACZ,IAAI,CAACC,EAAE,GAAGA,EAAE;IACZ,IAAI,CAACuB,IAAI,CAAC,IAAI,EAAE,IAAI,EAAEF,eAAe,EAAEC,QAAQ,CAAC;EACpD;EACA;AACJ;AACA;AACA;AACA;AACA;AACA;AACA;EACIC,IAAIA,CAAA,EAA6E;IAAA,IAA5EC,GAAG,GAAA9B,SAAA,CAAAC,MAAA,QAAAD,SAAA,QAAAE,SAAA,GAAAF,SAAA,MAAG,IAAI;IAAA,IAAEkB,YAAY,GAAAlB,SAAA,CAAAC,MAAA,QAAAD,SAAA,QAAAE,SAAA,GAAAF,SAAA,MAAG,IAAI;IAAA,IAAE2B,eAAe,GAAA3B,SAAA,CAAAC,MAAA,QAAAD,SAAA,QAAAE,SAAA,GAAAF,SAAA,MAAG,KAAK;IAAA,IAAE4B,QAAQ,GAAA5B,SAAA,CAAAC,MAAA,QAAAD,SAAA,QAAAE,SAAA,GAAAF,SAAA,MAAG,KAAK;IAC3E;IACA;IACA,MAAM+B,yBAAyB,GAAG,IAAI,CAACjC,KAAK,CAACkC,YAAY,CAACC,iBAAiB;IAC3E,IAAIC,GAAG,GAAG,IAAI;IACd,IAAI;MACA;MACA;MACA,IAAI,IAAI,CAACrB,aAAa,EAAE;QACpB,IAAI,CAACf,KAAK,CAACkC,YAAY,CAACC,iBAAiB,GAAG,CAACE,KAAK,EAAEC,OAAO,KAAK;UAC5DA,OAAO,CAACC,IAAI,CAACF,KAAK,CAACG,IAAI,CAACC,WAAW,CAAC,CAAC,CAAC;QAC1C,CAAC;MACL;MACA,IAAI,IAAI,CAACzB,aAAa,EAAE;QACpB,IAAI,CAAChB,KAAK,CAACkC,YAAY,CAACQ,WAAW,GAAG,CAACL,KAAK,EAAEM,OAAO,EAAEC,YAAY,EAAEC,YAAY,KAAK;UAClFF,OAAO,CAACG,OAAO,GAAGT,KAAK,CAACG,IAAI,CAACxC,KAAK,CAAC8C,OAAO;UAC1CH,OAAO,CAACJ,IAAI,CAACF,KAAK,CAACG,IAAI,CAACC,WAAW,CAAC,CAAC,CAAC;UACtC,OAAO,IAAI;QACf,CAAC;MACL;MACA,IAAI,CAACtB,GAAG,GAAGC,YAAY,IAAI,IAAI,GAAGA,YAAY,GAAG,IAAI,CAACD,GAAG;MACzD,IAAI4B,WAAW,GAAG,KAAK;MACvB,IAAI,IAAI,CAAC5B,GAAG,IAAI,IAAI,EAAE;QAClB4B,WAAW,GAAG,IAAI;QAClB,IAAI,CAAC5B,GAAG,GAAG6B,MAAM,CAACjB,IAAI,CAAC,CAAC;MAC5B;MACA,IAAI,CAAC,IAAI,CAACZ,GAAG,EAAE;QACX,MAAM,IAAI8B,KAAK,CAAC,+BAA+B,CAAC;MACpD;MACA,MAAMC,GAAG,GAAG,IAAI,CAAC/B,GAAG,CAACgC,QAAQ;MAC7B,IAAIJ,WAAW,EAAE;QACb,MAAMpB,EAAE,GAAG,IAAI,CAACD,UAAU,CAAC,CAAC;QAC5B,IAAIC,EAAE,IAAI,IAAI,IAAIA,EAAE,CAACxB,MAAM,GAAG,CAAC,EAAE;UAC7B+C,GAAG,CAACE,OAAO,CAACzB,EAAE,CAAC;QACnB;QACA,IAAIwB,QAAQ,CAACE,UAAU,KAAK,YAAY,EAAE;UACtCH,GAAG,CAACE,OAAO,CAAC,iBAAiB,CAAC;QAClC;QACAF,GAAG,CAACE,OAAO,CAAC,QAAQ,CAAC;QACrBF,GAAG,CAACE,OAAO,CAAC,QAAQ,CAAC;QACrB,IAAIpB,GAAG,EAAE;UACL,IAAI,CAACsB,SAAS,CAACJ,GAAG,EAAElB,GAAG,CAAC;QAC5B;QACAkB,GAAG,CAACE,OAAO,CAAC,SAAS,CAAC;QACtBF,GAAG,CAACE,OAAO,CAAC,uBAAuB,CAAC;MACxC;MACA;MACA,MAAMG,MAAM,GAAG,IAAI,CAACvD,KAAK,CAACwD,cAAc,CAAC,CAAC,CAACC,KAAK,CAAC,CAAC;MAClD,MAAMC,YAAY,GAAG,IAAI,CAAC1D,KAAK,CAAC2D,OAAO,CAAC,CAAC,CAACC,QAAQ,CAAC,CAAC;MACpD,MAAMC,EAAE,GAAGH,YAAY,GAAG,IAAI,CAACzD,KAAK;MACpC,MAAM6D,EAAE,GAAG,IAAI,CAAC9D,KAAK,CAAC2D,OAAO,CAAC,CAAC,CAACI,YAAY,CAAC,CAAC;MAC9C;MACA,IAAI,CAAC,IAAI,CAACjD,UAAU,EAAE;QAClB,IAAI,CAACP,EAAE,IAAIuD,EAAE,CAACE,CAAC,GAAG,IAAI,CAAC/D,KAAK;QAC5B,IAAI,CAACO,EAAE,IAAIsD,EAAE,CAACG,CAAC,GAAG,IAAI,CAAChE,KAAK;QAC5BsD,MAAM,CAACW,KAAK,IAAIX,MAAM,CAACS,CAAC;QACxBT,MAAM,CAACY,MAAM,IAAIZ,MAAM,CAACU,CAAC;QACzBV,MAAM,CAACS,CAAC,GAAG,CAAC;QACZT,MAAM,CAACU,CAAC,GAAG,CAAC;QACZ,IAAI,CAAC3D,MAAM,GAAG,CAAC;MACnB;MACA;MACA,MAAM8D,cAAc,GAAG,IAAI,CAAC/D,UAAU,CAAC6D,KAAK,GAAG,IAAI,CAAC5D,MAAM,GAAG,CAAC;MAC9D,MAAM+D,eAAe,GAAG,IAAI,CAAChE,UAAU,CAAC8D,MAAM,GAAG,IAAI,CAAC7D,MAAM,GAAG,CAAC;MAChE;MACA,IAAI,CAACD,UAAU,CAAC8D,MAAM,IAAI,IAAI,CAACvD,SAAS,GAAG,IAAI,CAACC,YAAY;MAC5D;MACA;MACA0C,MAAM,CAACW,KAAK,IAAIL,EAAE;MAClBN,MAAM,CAACY,MAAM,IAAIN,EAAE;MACnB,MAAMS,MAAM,GAAGC,IAAI,CAACC,GAAG,CAAC,CAAC,EAAED,IAAI,CAACE,IAAI,CAAC,CAAClB,MAAM,CAACW,KAAK,GAAG,IAAI,CAAC3D,EAAE,IAAI6D,cAAc,CAAC,CAAC;MAChF,MAAMM,MAAM,GAAGH,IAAI,CAACC,GAAG,CAAC,CAAC,EAAED,IAAI,CAACE,IAAI,CAAC,CAAClB,MAAM,CAACY,MAAM,GAAG,IAAI,CAAC3D,EAAE,IAAI6D,eAAe,CAAC,CAAC;MAClF,IAAI,CAAChD,SAAS,GAAGiD,MAAM,GAAGI,MAAM;MAChC,MAAMC,iBAAiB,GAAGA,CAAA,KAAM;QAC5B,IAAI,IAAI,CAAChE,YAAY,KAAK+D,MAAM,GAAG,CAAC,IAAIJ,MAAM,GAAG,CAAC,CAAC,EAAE;UACjD,MAAMM,KAAK,GAAG,IAAI,CAACC,kBAAkB,CAACH,MAAM,EAAEJ,MAAM,CAAC;UACrDpB,GAAG,CAAC4B,IAAI,CAACC,WAAW,CAACH,KAAK,CAAC;QAC/B;MACJ,CAAC;MACD,MAAMI,OAAO,GAAGA,CAAC5C,GAAG,EAAE6C,QAAQ,KAAK;QAC/B;QACA,IAAI,IAAI,CAACxE,WAAW,IAAI,IAAI,EAAE;UAC1B2B,GAAG,CAAC8C,KAAK,CAACzE,WAAW,GAAG,IAAI,CAACA,WAAW;UACxC2B,GAAG,CAAC8C,KAAK,CAACC,WAAW,GAAG,OAAO;UAC/B/C,GAAG,CAAC8C,KAAK,CAACE,WAAW,GAAG,KAAK;QACjC;QACA;QACA;QACAhD,GAAG,CAAC8C,KAAK,CAACG,UAAU,GAAG,IAAI,CAACnE,eAAe;QAC3C,IAAIW,eAAe,IAAIoD,QAAQ,EAAE;UAC7B7C,GAAG,CAAC8C,KAAK,CAACI,cAAc,GAAG,QAAQ;QACvC;QACA;QACA;QACA;QACA;QACA;QACA;QACA;QACA;QACA,IAAIvC,WAAW,IAAItD,MAAM,CAAC8F,OAAO,EAAE;UAC/B;UACA;UACA;UACA;UACArC,GAAG,CAACE,OAAO,CAAChB,GAAG,CAACoD,SAAS,CAAC;UAC1BpD,GAAG,CAACqD,UAAU,CAACC,WAAW,CAACtD,GAAG,CAAC;QACnC,CAAC,MACI,IAAI3C,MAAM,CAAC8F,OAAO,EAAE;UACrB,IAAI9B,KAAK,GAAGP,GAAG,CAACyC,aAAa,CAAC,KAAK,CAAC;UACpClC,KAAK,CAACmC,SAAS,GAAGxD,GAAG,CAACoD,SAAS;UAC/B/B,KAAK,GAAGA,KAAK,CAACoC,oBAAoB,CAAC,KAAK,CAAC,CAAC,CAAC,CAAC;UAC5C3C,GAAG,CAAC4B,IAAI,CAACC,WAAW,CAACtB,KAAK,CAAC;UAC3BrB,GAAG,CAACqD,UAAU,CAACC,WAAW,CAACtD,GAAG,CAAC;QACnC,CAAC,MACI;UACDA,GAAG,CAACqD,UAAU,CAACC,WAAW,CAACtD,GAAG,CAAC;UAC/Bc,GAAG,CAAC4B,IAAI,CAACC,WAAW,CAAC3C,GAAG,CAAC;QAC7B;QACA,IAAIP,eAAe,IAAIoD,QAAQ,EAAE;UAC7B,IAAI,CAACa,YAAY,CAAC5C,GAAG,CAAC;QAC1B;MACJ,CAAC;MACD,MAAM6C,GAAG,GAAG,IAAI,CAACC,aAAa,CAAC,IAAI,CAAC3F,UAAU,CAAC6D,KAAK,EAAE,IAAI,CAAC7D,UAAU,CAAC8D,MAAM,CAAC;MAC7E,IAAI4B,GAAG,IAAI,IAAI,EAAE;QACb,KAAK,IAAIE,CAAC,GAAG,CAAC,EAAEA,CAAC,GAAGF,GAAG,CAAC5F,MAAM,EAAE8F,CAAC,IAAI,CAAC,EAAE;UACpCjB,OAAO,CAACe,GAAG,CAACE,CAAC,CAAC,EAAE,IAAI,CAAC;QACzB;MACJ;MACA,MAAMC,GAAG,GAAG,IAAI,CAACC,aAAa,CAAC,IAAI,CAAC9F,UAAU,CAAC6D,KAAK,EAAE,IAAI,CAAC7D,UAAU,CAAC8D,MAAM,CAAC;MAC7E;MACA;MACA,KAAK,IAAI8B,CAAC,GAAG,CAAC,EAAEA,CAAC,GAAGvB,MAAM,EAAEuB,CAAC,IAAI,CAAC,EAAE;QAChC,MAAMG,EAAE,GAAIH,CAAC,GAAG5B,eAAe,GAAI,IAAI,CAACpE,KAAK,GACzC,IAAI,CAACO,EAAE,GAAG,IAAI,CAACP,KAAK,GACpB,CAACsD,MAAM,CAACU,CAAC,GAAGH,EAAE,CAACG,CAAC,GAAGP,YAAY,IAAIA,YAAY;QACnD,KAAK,IAAI2C,CAAC,GAAG,CAAC,EAAEA,CAAC,GAAG/B,MAAM,EAAE+B,CAAC,EAAE,EAAE;UAC7B,IAAI,IAAI,CAAClF,GAAG,IAAI,IAAI,EAAE;YAClB,OAAO,IAAI;UACf;UACA,MAAMmF,EAAE,GAAID,CAAC,GAAGjC,cAAc,GAAI,IAAI,CAACnE,KAAK,GACxC,IAAI,CAACM,EAAE,GAAG,IAAI,CAACN,KAAK,GACpB,CAACsD,MAAM,CAACS,CAAC,GAAGF,EAAE,CAACE,CAAC,GAAGN,YAAY,IAAIA,YAAY;UACnD,MAAM6C,OAAO,GAAGN,CAAC,GAAG3B,MAAM,GAAG+B,CAAC,GAAG,CAAC;UAClC,MAAMG,IAAI,GAAG,IAAIlH,SAAS,CAACgH,EAAE,EAAEF,EAAE,EAAEhC,cAAc,EAAEC,eAAe,CAAC;UACnEjC,GAAG,GAAG,IAAI,CAACqE,UAAU,CAAC,IAAI,CAACpG,UAAU,CAAC6D,KAAK,EAAE,IAAI,CAAC7D,UAAU,CAAC8D,MAAM,EAAE,CAAC,EAAE,CAAC,EAAG/B,GAAG,IAAK;YAChF,IAAI,CAACsE,gBAAgB,CAAC,CAACJ,EAAE,EAAE,CAACF,EAAE,EAAE,IAAI,CAACnG,KAAK,EAAEsG,OAAO,EAAEnE,GAAG,EAAEoE,IAAI,CAAC;YAC/D,IAAI,IAAI,CAACvF,oBAAoB,EAAE;cAC3B,IAAI,CAAC0F,qBAAqB,CAACvE,GAAG,EAAE,CAACkE,EAAE,EAAE,CAACF,EAAE,CAAC;YAC7C;UACJ,CAAC,EAAEG,OAAO,CAAC;UACX;UACAnE,GAAG,CAACwE,YAAY,CAAC,IAAI,YAAAC,MAAA,CAAYN,OAAO,CAAE,CAAC;UAC3CvB,OAAO,CAAC5C,GAAG,EAAE8D,GAAG,IAAI,IAAI,IAAID,CAAC,GAAGvB,MAAM,GAAG,CAAC,IAAI2B,CAAC,GAAG/B,MAAM,GAAG,CAAC,CAAC;QACjE;MACJ;MACA,IAAI4B,GAAG,IAAI,IAAI,EAAE;QACb,KAAK,IAAID,CAAC,GAAG,CAAC,EAAEA,CAAC,GAAGC,GAAG,CAAC/F,MAAM,EAAE8F,CAAC,IAAI,CAAC,EAAE;UACpCjB,OAAO,CAACkB,GAAG,CAACD,CAAC,CAAC,EAAEA,CAAC,GAAGC,GAAG,CAAC/F,MAAM,GAAG,CAAC,CAAC;QACvC;MACJ;MACA,IAAI4C,WAAW,IAAI,CAACjB,QAAQ,EAAE;QAC1B,IAAI,CAACgF,aAAa,CAAC,CAAC;QACpBnC,iBAAiB,CAAC,CAAC;MACvB;MACA,IAAI,CAACxD,GAAG,CAAC4F,KAAK,CAAC,CAAC;IACpB,CAAC,CACD,OAAOC,CAAC,EAAE;MACN;MACA,IAAI5E,GAAG,IAAI,IAAI,IAAIA,GAAG,CAACqD,UAAU,IAAI,IAAI,EAAE;QACvCrD,GAAG,CAACqD,UAAU,CAACC,WAAW,CAACtD,GAAG,CAAC;MACnC;IACJ,CAAC,SACO;MACJ,IAAI,CAACpC,KAAK,CAACkC,YAAY,CAACC,iBAAiB,GAAGF,yBAAyB;IACzE;IACA,OAAO,IAAI,CAACd,GAAG;EACnB;EACA;AACJ;AACA;EACI2E,YAAYA,CAAC5C,GAAG,EAAE;IACd,MAAM+D,EAAE,GAAG/D,GAAG,CAACyC,aAAa,CAAC,IAAI,CAAC;IAClCsB,EAAE,CAACC,SAAS,GAAG,aAAa;IAC5BhE,GAAG,CAAC4B,IAAI,CAACC,WAAW,CAACkC,EAAE,CAAC;EAC5B;EACA;AACJ;AACA;EACIH,aAAaA,CAAA,EAAG;IACZ,IAAI;MACA,IAAI,IAAI,CAAC3F,GAAG,IAAI,IAAI,IAAI,IAAI,CAACA,GAAG,CAACgC,QAAQ,IAAI,IAAI,EAAE;QAC/C,MAAMD,GAAG,GAAG,IAAI,CAAC/B,GAAG,CAACgC,QAAQ;QAC7B,IAAI,CAACgE,YAAY,CAACjE,GAAG,CAAC;QACtBA,GAAG,CAACE,OAAO,CAAC,SAAS,CAAC;QACtBF,GAAG,CAACE,OAAO,CAAC,SAAS,CAAC;QACtBF,GAAG,CAACkE,KAAK,CAAC,CAAC;QACX;QACA5H,aAAa,CAAC6H,OAAO,CAACnE,GAAG,CAAC4B,IAAI,CAAC;MACnC;IACJ,CAAC,CACD,OAAOkC,CAAC,EAAE;MACN;IAAA;EAER;EACA;AACJ;AACA;AACA;EACI1D,SAASA,CAACJ,GAAG,EAAElB,GAAG,EAAE;IAChB,IAAI,IAAI,CAACtB,KAAK,IAAI,IAAI,EAAE;MACpBwC,GAAG,CAACE,OAAO,WAAAyD,MAAA,CAAW,IAAI,CAACnG,KAAK,aAAU,CAAC;IAC/C;IACA;IACAd,aAAa,CAAC,YAAY,KAAAiH,MAAA,CAAKpH,MAAM,CAAC6H,QAAQ,sBAAmBpE,GAAG,CAAC;IACrE;IACAA,GAAG,CAACE,OAAO,CAAC,yBAAyB,CAAC;IACtCF,GAAG,CAACE,OAAO,CAAC,gBAAgB,CAAC;IAC7BF,GAAG,CAACE,OAAO,CAAC,4CAA4C,CAAC;IACzDF,GAAG,CAACE,OAAO,CAAC,2CAA2C,CAAC;IACxDF,GAAG,CAACE,OAAO,CAAC,qCAAqC,CAAC;IAClDF,GAAG,CAACE,OAAO,CAAC,GAAG,CAAC;IAChBF,GAAG,CAACE,OAAO,CAAC,iBAAiB,CAAC;IAC9B;IACA;IACAF,GAAG,CAACE,OAAO,CAAC,mEAAmE,GAC3E,iEAAiE,GACjE,gDAAgD,CAAC;IACrDF,GAAG,CAACE,OAAO,CAAC,oEAAoE,CAAC;IACjFF,GAAG,CAACE,OAAO,CAAC,qCAAqC,CAAC;IAClDF,GAAG,CAACE,OAAO,CAAC,GAAG,CAAC;IAChB,IAAIpB,GAAG,IAAI,IAAI,EAAE;MACbkB,GAAG,CAACE,OAAO,CAACpB,GAAG,CAAC;IACpB;IACAkB,GAAG,CAACE,OAAO,CAAC,UAAU,CAAC;EAC3B;EACA;AACJ;AACA;EACI+D,YAAYA,CAACjE,GAAG,EAAE;IACd;EAAA;EAEJ;AACJ;AACA;EACI2B,kBAAkBA,CAACH,MAAM,EAAEJ,MAAM,EAAE;IAC/B,IAAI,CAAC,IAAI,CAACnD,GAAG,EAAE;MACX,MAAM,IAAI8B,KAAK,CAAC,0BAA0B,CAAC;IAC/C;IACA,MAAMC,GAAG,GAAG,IAAI,CAAC/B,GAAG,CAACgC,QAAQ;IAC7B,MAAMyB,KAAK,GAAG1B,GAAG,CAACyC,aAAa,CAAC,OAAO,CAAC;IACxCf,KAAK,CAACsC,SAAS,GAAG,gBAAgB;IAClCtC,KAAK,CAACgC,YAAY,CAAC,QAAQ,EAAE,GAAG,CAAC;IACjC,MAAMW,KAAK,GAAGrE,GAAG,CAACyC,aAAa,CAAC,OAAO,CAAC;IACxC,KAAK,IAAIM,CAAC,GAAG,CAAC,EAAEA,CAAC,GAAGvB,MAAM,EAAEuB,CAAC,IAAI,CAAC,EAAE;MAChC,MAAMuB,GAAG,GAAGtE,GAAG,CAACyC,aAAa,CAAC,IAAI,CAAC;MACnC,KAAK,IAAIU,CAAC,GAAG,CAAC,EAAEA,CAAC,GAAG/B,MAAM,EAAE+B,CAAC,EAAE,EAAE;QAC7B,MAAME,OAAO,GAAGN,CAAC,GAAG3B,MAAM,GAAG+B,CAAC,GAAG,CAAC;QAClC,MAAMoB,IAAI,GAAGvE,GAAG,CAACyC,aAAa,CAAC,IAAI,CAAC;QACpC,MAAM+B,CAAC,GAAGxE,GAAG,CAACyC,aAAa,CAAC,GAAG,CAAC;QAChC+B,CAAC,CAACd,YAAY,CAAC,MAAM,aAAAC,MAAA,CAAaN,OAAO,CAAE,CAAC;QAC5C;QACA,IAAI9G,MAAM,CAACkI,KAAK,IAAI,CAAClI,MAAM,CAACmI,KAAK,IAAI,CAACnI,MAAM,CAACoI,KAAK,EAAE;UAChD,MAAMC,EAAE,iDAAAjB,MAAA,CAAiDN,OAAO,yDAAsD;UACtHmB,CAAC,CAACd,YAAY,CAAC,SAAS,EAAEkB,EAAE,CAAC;QACjC;QACAjI,KAAK,CAAC6H,CAAC,EAAEK,MAAM,CAACxB,OAAO,CAAC,CAAC;QACzBkB,IAAI,CAAC1C,WAAW,CAAC2C,CAAC,CAAC;QACnBF,GAAG,CAACzC,WAAW,CAAC0C,IAAI,CAAC;MACzB;MACAF,KAAK,CAACxC,WAAW,CAACyC,GAAG,CAAC;IAC1B;IACA5C,KAAK,CAACG,WAAW,CAACwC,KAAK,CAAC;IACxB,OAAO3C,KAAK;EAChB;EACA;AACJ;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;EACI6B,UAAUA,CAACuB,CAAC,EAAEC,CAAC,EAAE3B,EAAE,EAAEF,EAAE,EAAE8B,OAAO,EAAEC,UAAU,EAAE;IAC1C,IAAI/F,GAAG,GAAGe,QAAQ,CAACwC,aAAa,CAAC,KAAK,CAAC;IACvC,IAAIyC,GAAG,GAAG,IAAI;IACd,IAAI;MACA;MACA;MACA,IAAI9B,EAAE,KAAK,CAAC,IAAIF,EAAE,KAAK,CAAC,EAAE;QACtBhE,GAAG,CAAC8C,KAAK,CAACmD,QAAQ,GAAG,UAAU;QAC/BjG,GAAG,CAAC8C,KAAK,CAAChB,KAAK,MAAA2C,MAAA,CAAMmB,CAAC,OAAI;QAC1B5F,GAAG,CAAC8C,KAAK,CAACf,MAAM,MAAA0C,MAAA,CAAMoB,CAAC,OAAI;QAC3B7F,GAAG,CAAC8C,KAAK,CAACoD,eAAe,GAAG,OAAO;QACnC,MAAMC,QAAQ,GAAGpF,QAAQ,CAACwC,aAAa,CAAC,KAAK,CAAC;QAC9C4C,QAAQ,CAACrD,KAAK,CAACmD,QAAQ,GAAG,UAAU;QACpCE,QAAQ,CAACrD,KAAK,CAACsD,GAAG,MAAA3B,MAAA,CAAM,IAAI,CAACvG,MAAM,OAAI;QACvCiI,QAAQ,CAACrD,KAAK,CAACuD,IAAI,MAAA5B,MAAA,CAAM,IAAI,CAACvG,MAAM,OAAI;QACxCiI,QAAQ,CAACrD,KAAK,CAAChB,KAAK,MAAA2C,MAAA,CAAMmB,CAAC,GAAG,CAAC,GAAG,IAAI,CAAC1H,MAAM,OAAI;QACjDiI,QAAQ,CAACrD,KAAK,CAACf,MAAM,MAAA0C,MAAA,CAAMoB,CAAC,GAAG,CAAC,GAAG,IAAI,CAAC3H,MAAM,OAAI;QAClDiI,QAAQ,CAACrD,KAAK,CAACwD,QAAQ,GAAG,QAAQ;QAClC,MAAMC,QAAQ,GAAGxF,QAAQ,CAACwC,aAAa,CAAC,KAAK,CAAC;QAC9CgD,QAAQ,CAACzD,KAAK,CAACmD,QAAQ,GAAG,UAAU;QACpCM,QAAQ,CAACzD,KAAK,CAAC0D,UAAU,MAAA/B,MAAA,CAAMP,EAAE,OAAI;QACrCqC,QAAQ,CAACzD,KAAK,CAACtE,SAAS,MAAAiG,MAAA,CAAMT,EAAE,OAAI;QACpCmC,QAAQ,CAACxD,WAAW,CAAC4D,QAAQ,CAAC;QAC9BvG,GAAG,CAAC2C,WAAW,CAACwD,QAAQ,CAAC;QACzBpF,QAAQ,CAAC2B,IAAI,CAACC,WAAW,CAAC3C,GAAG,CAAC;QAC9BgG,GAAG,GAAGO,QAAQ;MAClB;MACA;MAAA,KACK;QACDvG,GAAG,CAAC8C,KAAK,CAAChB,KAAK,MAAA2C,MAAA,CAAMmB,CAAC,OAAI;QAC1B5F,GAAG,CAAC8C,KAAK,CAACf,MAAM,MAAA0C,MAAA,CAAMoB,CAAC,OAAI;QAC3B7F,GAAG,CAAC8C,KAAK,CAACwD,QAAQ,GAAG,QAAQ;QAC7BtG,GAAG,CAAC8C,KAAK,CAACoD,eAAe,GAAG,OAAO;QACnC,MAAMC,QAAQ,GAAGpF,QAAQ,CAACwC,aAAa,CAAC,KAAK,CAAC;QAC9C4C,QAAQ,CAACrD,KAAK,CAAChB,KAAK,MAAA2C,MAAA,CAAMmB,CAAC,GAAG,CAAC,GAAG,IAAI,CAAC1H,MAAM,OAAI;QACjDiI,QAAQ,CAACrD,KAAK,CAACf,MAAM,MAAA0C,MAAA,CAAMoB,CAAC,GAAG,CAAC,GAAG,IAAI,CAAC3H,MAAM,OAAI;QAClDiI,QAAQ,CAACrD,KAAK,CAACwD,QAAQ,GAAG,QAAQ;QAClCH,QAAQ,CAACrD,KAAK,CAACsD,GAAG,MAAA3B,MAAA,CAAM,IAAI,CAACvG,MAAM,OAAI;QACvCiI,QAAQ,CAACrD,KAAK,CAACuD,IAAI,MAAA5B,MAAA,CAAM,IAAI,CAACvG,MAAM,OAAI;QACxC8B,GAAG,CAAC2C,WAAW,CAACwD,QAAQ,CAAC;QACzBpF,QAAQ,CAAC2B,IAAI,CAACC,WAAW,CAAC3C,GAAG,CAAC;QAC9BgG,GAAG,GAAGG,QAAQ;MAClB;IACJ,CAAC,CACD,OAAOvB,CAAC,EAAE;MACN,IAAI5E,GAAG,IAAIA,GAAG,CAACqD,UAAU,EAAE;QACvBrD,GAAG,CAACqD,UAAU,CAACC,WAAW,CAACtD,GAAG,CAAC;MACnC;MACAA,GAAG,GAAG,IAAI;MACV,MAAM4E,CAAC;IACX;IACAkB,OAAO,CAACE,GAAG,CAAC;IACZ,OAAOhG,GAAG;EACd;EACA;AACJ;AACA;EACIyG,OAAOA,CAAA,EAAG;IACN,IAAIC,IAAI,GAAG,IAAI,CAAC9I,KAAK,CAACwC,IAAI,CAACuG,WAAW;IACtC,IAAID,IAAI,IAAI,IAAI,EAAE;MACdA,IAAI,GAAG,IAAI,CAAC9I,KAAK,CAACgJ,YAAY,CAAC,CAAC,CAACH,OAAO,CAAC,CAAC;IAC9C;IACA,OAAOC,IAAI;EACf;EACA;AACJ;AACA;AACA;AACA;EACIG,gBAAgBA,CAAA,EAAG;IACf,OAAO,CAACxJ,MAAM,CAACyJ,KAAK,IAAI,CAACzJ,MAAM,CAACmI,KAAK;EACzC;EACA;AACJ;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;EACIlB,gBAAgBA,CAACJ,EAAE,EAAEF,EAAE,EAAEnG,KAAK,EAAEkI,UAAU,EAAE/F,GAAG,EAAEoE,IAAI,EAAE;IACnD,MAAMhE,IAAI,GAAG,IAAI,CAACxC,KAAK,CAAC2D,OAAO,CAAC,CAAC;IACjC,MAAMwF,iBAAiB,GAAG,IAAI,CAACnJ,KAAK,CAACoJ,SAAS;IAC9C,IAAI,CAACpJ,KAAK,CAACoJ,SAAS,GAAGhH,GAAG;IAC1B,MAAMiH,MAAM,GAAG7G,IAAI,CAAC8G,SAAS,CAAC,CAAC;IAC/B,MAAMC,cAAc,GAAG/G,IAAI,CAACgH,iBAAiB,CAAC,CAAC;IAC/C,MAAMC,QAAQ,GAAGjH,IAAI,CAACC,WAAW,CAAC,CAAC;IACnC,MAAMiH,WAAW,GAAGlH,IAAI,CAACmH,cAAc,CAAC,CAAC;IACzC,MAAMC,SAAS,GAAG3J,KAAK;IACvB,IAAI,IAAI,CAACD,KAAK,CAAC8C,OAAO,KAAKnD,OAAO,CAACkK,GAAG,EAAE;MACpCrH,IAAI,CAACsH,SAAS,CAAC,CAAC;MAChB;MACA,IAAI,IAAI,CAACb,gBAAgB,CAAC,CAAC,EAAE;QACzB,MAAMc,CAAC,GAAGvH,IAAI,CAACC,WAAW,CAAC,CAAC,CAACgD,UAAU;QACvC,MAAMuE,IAAI,GAAGD,CAAC,CAACE,YAAY,CAAC,WAAW,CAAC;QACxCF,CAAC,CAACnD,YAAY,CAAC,iBAAiB,EAAE,KAAK,CAAC;QACxCmD,CAAC,CAACnD,YAAY,CAAC,WAAW,EAAE,SAAAC,MAAA,CAAS5G,KAAK,OAAA4G,MAAA,CAAI5G,KAAK,sBAAA4G,MAAA,CAAmBP,EAAE,OAAAO,MAAA,CAAIT,EAAE,MAAG,CAAC;QAClFnG,KAAK,GAAG,CAAC;QACTqG,EAAE,GAAG,CAAC;QACNF,EAAE,GAAG,CAAC;MACV;IACJ,CAAC,MACI;MACD5D,IAAI,CAAC0H,UAAU,CAAC,CAAC;IACrB;IACA;IACA,MAAMC,aAAa,GAAG3H,IAAI,CAAC4H,eAAe,CAAC,CAAC;IAC5C5H,IAAI,CAAC6H,gBAAgB,CAAC,KAAK,CAAC;IAC5B;IACA,MAAMC,YAAY,GAAG,IAAI,CAACtK,KAAK,CAACuK,SAAS,CAAC,CAAC;IAC3C,IAAI,CAACvK,KAAK,CAACwK,UAAU,CAAC,KAAK,CAAC;IAC5B;IACA,MAAMC,SAAS,GAAGjI,IAAI,CAACuB,YAAY,CAAC,CAAC;IACrCvB,IAAI,CAACiI,SAAS,GAAG,IAAIpL,KAAK,CAACiH,EAAE,EAAEF,EAAE,CAAC;IAClC;IACA,MAAM;MAAEsE;IAAO,CAAC,GAAG,IAAI,CAAC1K,KAAK,CAACkC,YAAY;IAC1C,MAAM;MAAEyI;IAAO,CAAC,GAAGnI,IAAI;IACvB,MAAMoI,CAAC,GAAGpI,IAAI,CAACvC,KAAK;IACpB;IACA,IAAI,IAAI,CAACqB,QAAQ,EAAE;MACf,MAAMuJ,QAAQ,GAAG,IAAIvL,SAAS,CAAC,CAACkH,IAAI,CAACxC,CAAC,GAAGyG,SAAS,CAACzG,CAAC,IAAI4G,CAAC,EAAE,CAACpE,IAAI,CAACvC,CAAC,GAAGwG,SAAS,CAACxG,CAAC,IAAI2G,CAAC,EAAGpE,IAAI,CAACtC,KAAK,GAAG0G,CAAC,GAAIhB,SAAS,EAAGpD,IAAI,CAACrC,MAAM,GAAGyG,CAAC,GAAIhB,SAAS,CAAC;MACnJ;MACA;MACA,IAAI,CAAC5J,KAAK,CAACkC,YAAY,CAACwI,MAAM,GAAG,CAACrI,KAAK,EAAEyI,KAAK,EAAEC,SAAS,KAAK;QAC1D,IAAI1I,KAAK,IAAI,IAAI,EAAE;UACf;UACA,MAAM2I,IAAI,GAAGL,MAAM,CAACM,GAAG,CAAC5I,KAAK,CAACoF,IAAI,CAAC;UACnC,IAAIuD,IAAI,IAAI,IAAI,EAAE;YACd,MAAME,IAAI,GAAG1I,IAAI,CAAC2I,cAAc,CAACH,IAAI,EAAE,KAAK,CAAC;YAC7C;YACA;YACA,IAAIE,IAAI,IAAI,IAAI,IACZA,IAAI,CAAChH,KAAK,GAAG,CAAC,IACdgH,IAAI,CAAC/G,MAAM,GAAG,CAAC,IACf,CAACzE,UAAU,CAACmL,QAAQ,EAAEK,IAAI,CAAC,EAAE;cAC7B;YACJ;UACJ;QACJ;QACAR,MAAM,CAACU,KAAK,CAAC,IAAI,EAAE,CAAC/I,KAAK,EAAEyI,KAAK,EAAEC,SAAS,CAAC,CAAC,CAAC,CAAC;MACnD,CAAC;IACL;IACA,IAAIM,IAAI,GAAG,IAAI;IACf,IAAI;MACA;MACA;MACA,MAAMC,KAAK,GAAG,CAAC,IAAI,CAACzC,OAAO,CAAC,CAAC,CAAC;MAC9BwC,IAAI,GAAG,IAAI9L,mBAAmB,CAACiD,IAAI,EAAEvC,KAAK,EAAEqL,KAAK,EAAE,IAAI,EAAGjJ,KAAK,IAAK;QAChE,OAAO,IAAI,CAACkJ,mBAAmB,CAAClJ,KAAK,CAAC;MAC1C,CAAC,CAAC;IACN,CAAC,SACO;MACJ;MACA,IAAImJ,GAAG,GAAGpJ,GAAG,CAACqJ,UAAU;MACxB,OAAOD,GAAG,IAAI,IAAI,EAAE;QAChB,MAAME,IAAI,GAAGF,GAAG,CAACG,WAAW;QAC5B,MAAMC,IAAI,GAAGJ,GAAG,CAACK,QAAQ,CAACC,WAAW,CAAC,CAAC;QACvC;QACA,IAAIF,IAAI,KAAK,KAAK,EAAE;UAChBJ,GAAG,CAACtG,KAAK,CAACwD,QAAQ,GAAG,QAAQ;UAC7B8C,GAAG,CAACtG,KAAK,CAACmD,QAAQ,GAAG,UAAU;UAC/BmD,GAAG,CAACtG,KAAK,CAACsD,GAAG,MAAA3B,MAAA,CAAM,IAAI,CAACjG,SAAS,OAAI;UACrC4K,GAAG,CAAC5E,YAAY,CAAC,OAAO,EAAEmB,MAAM,CAACvB,IAAI,CAACtC,KAAK,CAAC,CAAC;UAC7CsH,GAAG,CAAC5E,YAAY,CAAC,QAAQ,EAAEmB,MAAM,CAACvB,IAAI,CAACrC,MAAM,CAAC,CAAC;UAC/CqH,GAAG,CAACtG,KAAK,CAAChB,KAAK,GAAG,EAAE;UACpBsH,GAAG,CAACtG,KAAK,CAACf,MAAM,GAAG,EAAE;QACzB;QACA;QAAA,KACK,IAAIqH,GAAG,CAACtG,KAAK,CAAC6G,MAAM,KAAK,SAAS,IAAIH,IAAI,KAAK,KAAK,EAAE;UACvDJ,GAAG,CAAC/F,UAAU,CAACC,WAAW,CAAC8F,GAAG,CAAC;QACnC;QACAA,GAAG,GAAGE,IAAI;MACd;MACA;MACA,IAAI,IAAI,CAACzK,oBAAoB,EAAE;QAC3B,MAAM+K,IAAI,GAAG5J,GAAG,CAACyD,oBAAoB,CAAC,KAAK,CAAC;QAC5C,IAAImG,IAAI,CAAC7L,MAAM,GAAG,CAAC,EAAE;UACjB6L,IAAI,CAAC,CAAC,CAAC,CAAC9G,KAAK,CAACmD,QAAQ,GAAG,UAAU;QACvC;MACJ;MACA;MACA7F,IAAI,CAACkH,WAAW,CAACjE,UAAU,CAACC,WAAW,CAAClD,IAAI,CAACkH,WAAW,CAAC;MACzD;MACA,IAAI,CAAC1J,KAAK,CAACwK,UAAU,CAACF,YAAY,CAAC;MACnC,IAAI,CAACtK,KAAK,CAACoJ,SAAS,GAAGD,iBAAiB;MACxC,IAAI,CAACnJ,KAAK,CAACkC,YAAY,CAACwI,MAAM,GAAGA,MAAM;MACvClI,IAAI,CAAC6G,MAAM,GAAGA,MAAM;MACpB7G,IAAI,CAAC+G,cAAc,GAAGA,cAAc;MACpC/G,IAAI,CAACiH,QAAQ,GAAGA,QAAQ;MACxBjH,IAAI,CAACkH,WAAW,GAAGA,WAAW;MAC9BlH,IAAI,CAACiI,SAAS,GAAGA,SAAS;MAC1B,IAAIY,IAAI,EAAE;QACNA,IAAI,CAACY,OAAO,CAAC,CAAC;MAClB;MACAzJ,IAAI,CAAC6H,gBAAgB,CAACF,aAAa,CAAC;IACxC;EACJ;EACA;AACJ;AACA;EACIoB,mBAAmBA,CAAClJ,KAAK,EAAE;IACvB,OAAO,IAAI,CAACrC,KAAK,CAACkM,cAAc,CAAC7J,KAAK,CAACoF,IAAI,CAAC;EAChD;EACA;AACJ;AACA;EACId,qBAAqBA,CAACvE,GAAG,EAAEkE,EAAE,EAAEF,EAAE,EAAE;IAC/B,MAAM+F,EAAE,GAAG,IAAI,CAACnM,KAAK,CAACoM,eAAe;IACrC,IAAID,EAAE,IAAI,IAAI,EAAE;MACZ,MAAME,GAAG,GAAGlJ,QAAQ,CAACwC,aAAa,CAAC,KAAK,CAAC;MACzC0G,GAAG,CAACnH,KAAK,CAACmD,QAAQ,GAAG,UAAU;MAC/BgE,GAAG,CAACnH,KAAK,CAAC0D,UAAU,MAAA/B,MAAA,CAAMtC,IAAI,CAAC+H,KAAK,CAAChG,EAAE,GAAG,IAAI,CAACrG,KAAK,CAAC,OAAI;MACzDoM,GAAG,CAACnH,KAAK,CAACtE,SAAS,MAAAiG,MAAA,CAAMtC,IAAI,CAAC+H,KAAK,CAAClG,EAAE,GAAG,IAAI,CAACnG,KAAK,CAAC,OAAI;MACxDoM,GAAG,CAACzF,YAAY,CAAC,OAAO,EAAEmB,MAAM,CAACxD,IAAI,CAAC+H,KAAK,CAAC,IAAI,CAACrM,KAAK,GAAGkM,EAAE,CAACjI,KAAK,CAAC,CAAC,CAAC;MACpEmI,GAAG,CAACzF,YAAY,CAAC,QAAQ,EAAEmB,MAAM,CAACxD,IAAI,CAAC+H,KAAK,CAAC,IAAI,CAACrM,KAAK,GAAGkM,EAAE,CAAChI,MAAM,CAAC,CAAC,CAAC;MACtEkI,GAAG,CAACE,GAAG,GAAGJ,EAAE,CAACI,GAAG;MAChBnK,GAAG,CAACoK,YAAY,CAACH,GAAG,EAAEjK,GAAG,CAACqJ,UAAU,CAAC;IACzC;EACJ;EACA;AACJ;AACA;EACIzF,aAAaA,CAAC9B,KAAK,EAAEC,MAAM,EAAE;IACzB,OAAO,IAAI;EACf;EACA;AACJ;AACA;EACIgC,aAAaA,CAACjC,KAAK,EAAEC,MAAM,EAAE;IACzB,OAAO,IAAI;EACf;EACA;AACJ;AACA;AACA;AACA;EACIsI,KAAKA,CAACzK,GAAG,EAAE;IACP,MAAMb,GAAG,GAAG,IAAI,CAACY,IAAI,CAACC,GAAG,CAAC;IAC1B,IAAIb,GAAG,IAAI,IAAI,EAAE;MACbA,GAAG,CAACsL,KAAK,CAAC,CAAC;IACf;EACJ;EACA;AACJ;AACA;EACIrF,KAAKA,CAAA,EAAG;IACJ,IAAI,IAAI,CAACjG,GAAG,IAAI,IAAI,EAAE;MAClB,IAAI,CAACA,GAAG,CAACiG,KAAK,CAAC,CAAC;MAChB,IAAI,CAACjG,GAAG,GAAG,IAAI;IACnB;EACJ;AACJ;AACA,eAAerB,YAAY","ignoreList":[]},"metadata":{},"sourceType":"module","externalDependencies":[]}