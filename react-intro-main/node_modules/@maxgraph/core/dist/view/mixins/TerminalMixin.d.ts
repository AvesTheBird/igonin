import Cell from '../cell/Cell';
declare module '../Graph' {
    interface Graph {
        isTerminalPointMovable: (cell: Cell, source: boolean) => boolean;
        getOpposites: (edges: Cell[], terminal: Cell | null, sources?: boolean, targets?: boolean) => Cell[];
    }
}
